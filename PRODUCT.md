# Roadmap

Owned by the planning role. One item per iteration, smallest useful increment first. Ship order is dependency order.

| # | Item | Status | Notes |
|---|---|---|---|
| 1 | Gap schema, closed taxonomy, evidence ladder | shipped | `models.py`, `taxonomy.py` |
| 2 | Registry loader with duplicate + schema gates | shipped | `registry.py` |
| 3 | Two-axis scoring, no blending, visible floor | shipped | `scoring.py` |
| 4 | Ranked report + per-gap brief renderers | shipped | `render.py` |
| 5 | Build-loop PRD emitter | shipped | `prd.py`, red-test-first stories |
| 6 | CLI: validate, list, report, show, prd, taxonomy | shipped | `cli.py` |
| 7 | Seed records with resolvable citations | shipped | `gaps/`. Counts are not restated here: run `radar report .` |
| 8 | Byte-exact golden report fixtures | open | Pin renderer output against committed goldens |
| 9 | Below-floor rows name the cheapest source class that would lift them to the floor | shipped | `scoring.promotion_options` + a derived `Needs` column; no verb, so `tools/promote.py` keeps the word |
| 10 | Evidence staleness: flag records whose newest citation is older than N months | shipped | Iteration 83. A gap can be closed by the industry without anyone noticing, and `models.Evidence.date` was enforced on every record while nothing aggregated it -- two pass-through reads in `src/` (`render.py`, `prd.py`) and zero in `tools/`. Measured before speccing, anchored on the register's OWN newest citation date because a census of `date.today()`, `datetime.now(`, `time.time()` and `utcnow` across `src/`+`tests/`+`tools/` returns ZERO hits and the quality bar demands committable bytes: 48 records past 6 months, 16 past 12, 1 past 18, stalest GAP-078 at 1,253 days; and GAP-016 renders at rank 3, priority 9.0, confidence 5 on 4 citations whose newest is 2025-07-23 (401 days) while its own status reads `partially-addressed`, one of only 3 non-`open` records. The 365-day threshold is argued from that distribution rather than felt: 6 months selects 40% of the register, which is a wall and not a call to action, and 18 months selects one record. Ships as `## Evidence age` between `## By layer` and `## Below confidence floor`, a position chosen by measurement -- iteration 17's `_B8_AFTER` pins the below-floor section to end-of-document and that module's `_section()` slices to the next `## `, so no committed byte-pin is re-baselined -- but iteration 08's document-wide `exactly once` id count IS re-baselined, its span narrowed to the two partition sections, because that count was a PROXY for the never-drop partition and any further id-bearing section reds it; a placement census scoped to byte pins does not see document-wide behavior pins. Display only: no age term reaches `confidence`, `priority`, `rank` or `below_floor`, nothing is dropped, and no rendered line carries the word `stale`, because a judgment word invites padding a sound record with a fresher link instead of going to check the source |
| 11 | Cross-record duplicate detection on normalised title tokens | open | Prevent two records describing one gap |
| 12 | `radar diff` between two register states | shipped | `diff.py`. Nine CLOSED fields are compared (`status`, `layer`, `gap_type`, `severity`, `frequency`, `tractability`, `priority`, `confidence`, citation count) and free prose never is, so a rewording cannot bury a score move. `priority` and `confidence` are CALLED from `scoring`, never re-derived here, so this report cannot disagree with the ranking. Both domain sizes are stated and both paths are REQUIRED, so an emptied, moved or one-level-too-high side cannot read as "everything was added" |
| 13 | Layer coverage report: which layers have no records and are therefore unexamined | shipped | `render.py` dropped zero-count layers with a trailing `if n`, so the section rendered 9 data rows against the 11 closed layers and hid `cost-governance` and `human-interface` -- the two layers that have never held a record, which is the one fact a coverage view exists to publish. Same omission-by-filter shape as iter 01's `radar list` fix, against the never-drop rule `VISION.md` names as the rule it protects. The omission was PINNED AS INTENT by `test_report_layer_table_only_lists_populated_layers` (`assert "cost-governance" not in layer_block`), so this iteration reverses a committed expectation rather than fixing an unasserted accident. A deliberate byte change to the primary HUMAN surface, and the only one since iter 05. A purpose line under the heading states that a zero means UNEXAMINED and is not a target to fill, because a printed zero otherwise invites the "fill every layer" coverage-throughput reading `docs/CONSUMER_CONTRACT.md` forbids by name -- the section now carries its own reason exactly as `## Below confidence floor` already does. Row count is asserted against `taxonomy.layer_names()` and never against a literal 11, and the new tests are synthetic-only, so the recurring research pass cannot red them (the iter-09 landmine). Goldens (row 8) must be cut AFTER this row, not before |
| 14 | `radar list` displays below-floor records, flagged; adds `--json` | shipped | `list` dropped them, breaking the one rule VISION.md protects by name |
| 15 | "Strongest source" must come from the ladder rung, not the alphabet | shipped | `scoring.strongest_source`, keyed on the `SOURCE_CLASSES` index so equal weights resolve by rung. The alphabetical `min()` was wrong for GAP-013 and GAP-016 at `--floor 6`, and worse at the DEFAULT floor: `confidence() < 2` restricts every below-floor row to `{secondary-summary, model-output}`, the one pair that mis-sorts, so the table could name `model-output` as a record's best evidence |
| 16 | `radar list --layer L` filter | shipped | Iteration 84. Deliberately split from #14: a filter is an omission mechanism and needs the never-drop semantics settled first -- and that reason is DISCHARGED by work already shipped, which is why the row could finally land: iteration 01 stopped `radar list` dropping below-floor records, and iteration 08 (row 26) collapsed `rank()` and `below_floor()` into ONE single-pass partition, so never-dropping stopped being a coincidence. The filter therefore narrows the LOADED RECORD DOMAIN before `_list_rows` and adds no second below-floor predicate, and never-drop is asserted at document level: the 11 per-layer documents concatenate to the unfiltered line multiset exactly. Unknown layer = zero stdout bytes, `Error: ` and exit 2 with the vocabulary derived from `taxonomy.LAYERS`, validated before the register loads; a valid layer holding zero records inherits `document([])`. Publishing WHICH filter produced a `--json` payload is row 81, not this row |
| 17 | `radar scan --prd` honours the confidence floor its twin `radar prd` already enforces | shipped | The report displays below-floor findings; the PRD selection must not BUILD against one |
| 18 | `radar scan` drives its file walk from the tracked set instead of globbing the tree | shipped | 12x faster on a 230-file target; `checks.py` |
| 19 | Roadmap-ledger integrity: retire the ambiguous `iter NN` status, brake on a shipped iteration with no ledger row | shipped | The loss of iter 03 was invisible while the status column carried three vocabularies |
| 20 | `docs/CONSUMER_CONTRACT.md` restates a record count it does not derive, and is wrong by 60% | shipped | The document a machine consumer reads breaks the restated-count rule `README.md` publishes. STATUS CORRECTED in iteration 94, not built: the defect was fixed OUTSIDE the loop by commit `e352240` and this cell was never flipped. Re-verified by iteration 94's scout B over all 407 lines of the contract -- 8 lines mention both a number and the word `record` and every one is a schema or surface description, so no restated register count survives. What was owed here was a status decision, and a stale `open` row is not inert: `Next up` may name only `open` rows, so it stays an ELIGIBLE forward candidate and the loop can spend an iteration re-fixing a defect that no longer exists. Nothing in `tools/roadmap_integrity.py` tests a row's defect against the TREE -- every check reads the document against itself or against git subjects -- which is the class new row 91 exists to narrow |
| 21 | `radar list --json` carries the below-floor prescription a gate can assert | open | Split from #9, which landed the human table first; a marker can be scraped, a field can be asserted |
| 22 | `radar validate` must fail when it examined zero records | shipped | Exit 0 over an empty domain made the `iff` published at `docs/CONSUMER_CONTRACT.md` false; `tools/check_locators.py` already refused the identical case and `radar prd` already exits 2 |
| 23 | `radar list` over a zero-record register writes zero bytes, not one newline | shipped | Iteration 66. The product had already decided the question in code -- `render.document([])` returns a newline -- and `list` missed it only because `cli._list_text` was a THIRD private construction of the tail rows 32 and 46 collapsed twice before, this one built by appending per line (`"".join(f"..." + newline for ...)`), which returns the empty string over zero rows. Re-measured before speccing: across all 8 verbs over a zero-record register (10 invocations counting the two `--json` twins) exactly ONE exits 0 with 0 stdout bytes, and routing is byte-identical on the live register (1,889 bytes, 16 records, both forms equal). Row 46 predicted this class -- a census matching the source BODY reports a partial copy ABSENT -- so the brake shipped here is not a fourth lexical rule but an assertion over the BYTES of every exit-0 verb, which no new construction can evade |
| 24 | `radar scan` states how many records it applied | shipped | Both surfaces now publish the denominator every other number is relative to: the markdown brief gains `Register records applied: N` immediately above the no-check line plus an explicit vacuity statement when that number is 0, and `scan --json` gains `records_applied` between `confidence_floor` and `counts`. It is DERIVED from `len(findings) + len(uncheckable)`, so it cannot disagree with the collections it summarises, and a test asserts it equals `sum(counts.values()) + len(uncheckable)` at both register sizes. Measured before the change: an empty-register scan exited 0 with 685 bytes of all-zero census and three "None found." sections, indistinguishable from a clean target. Exit codes deliberately unmoved -- that is row 25, which this unblocks. This row lost triage TWICE on SHAPE (iters 16 and 18) and never once on merit; iteration 19 measured why -- see the Next-up paragraph -- and took it |
| 25 | `radar scan` gains an opt-in, floor-gated non-zero exit code | shipped | Iteration 67 spent the `1` this row reserved: `scan --exit-code` exits 1 when the target holds a PRESENT finding whose evidence clears the confidence floor, 0 when it holds none, and 2 when the scan applied zero records, with stdout byte-identical to the flagless invocation whenever it verdicts. The predicate is DERIVED from the same floor filter `--prd` uses, so the code and `--json`'s `below_floor` cannot disagree, and the two flags are mutually exclusive because they answer one question with opposite codes. Raised by independent scouts in iter 01 and again in iter 07: a scan finding 3 PRESENT gaps exits 0, so a gate must parse JSON to learn anything. Must stay opt-in with today's bytes unchanged, must gate on the confidence floor (a `model-output` record may never block a build), keep exit 2 for errors, and must never become a "gaps closed" score |
| 26 | `rank()` and `below_floor()` collapse into one single-pass partition, so never-dropping a record stops being a coincidence | shipped | The two filters were exact complements by inspection and by nothing else, while `VISION.md` names that rule as the one it protects and iter 01 shipped the fix for an actual instance of it in `radar list`. Output bytes unchanged; `cli.py:54` already carried a comment warning that a second below-floor predicate is how this breaks. The behavioral proxy for the dedup is a call count: `confidence()` is evaluated exactly once per record per call, where each half previously scored every record and then re-scored its survivors (measured 31 and 17 calls over 16 records) |
| 27 | Two live-register id censuses become oracles derived from `confidence()`, and the rendered report's SECTION membership is asserted for the first time | shipped | `test_iter05_behavior.py` pinned `sorted(cells) == ["GAP-008", "GAP-010"]`, and the recurring research pass falsified it by writing a 17th record: the suite went RED over a CORRECT register, which is how a loop comes to revert something unrelated. Iteration 08 proved every id appears exactly once in `radar report`; nothing proved it appears in the RIGHT section, so a below-floor record rendered into the ranked table would still have passed. Zero-line `src/` diff; `gaps/` untouched, because the records are another writer's in-flight data |
| 28 | A suite brake against re-introducing a closed-set id census over the live register | open | Row 27 removed the two that existed; nothing stops the next one, and the landmine only detonates days later when a research pass lands. Must not fire on the synthetic `tmp_path` fixtures, which name ids legitimately. Iteration 31's scout measured BOTH cheap forms and both are wrong in OPPOSITE directions, so the obvious spec is dead: an AST rule keying on an `==` against an id-literal collection fires on 7 sites of which 6 are legitimate synthetic `tmp_path` fixtures (`test_iter05_behavior.py:411`, `test_iter11_behavior.py:404` and `:437`, `test_registry_and_render.py:41` and `:47`, `test_scoring.py:88`), and excluding functions that take `tmp_path` drops it to 1 -- itself a false positive over in-memory objects -- while EXEMPTING the one genuinely live-derived census, `test_iter27_behavior.py:293`, whose copied register is written into `tmp_path`. The id literal is not the deciding property; WHICH register the collection came from is, and that is dataflow neither rule decides. The honest shape is a shared `live_ids()` test helper plus a brake scoped to functions that call it. A real known-bad sample survives in history: `git show 1e00bfe^:tests/test_iter05_behavior.py` carries the removed census at line 294 |
| 29 | `radar scan` reads each file at most once per scan | shipped | `checks._read` was called once per (rule, file) pair. RE-MEASURED in iter 15 on agent-foundry (231 tracked files), replacing this row's stale iter-09 figures: 4,461 reads over 226 distinct files (19.7x amplification), 98.3 MB decoded to cover 4.3 MB of distinct text, the hottest three files decoded 32 times each. The fix is a cache whose lifetime is exactly one `scan()`, so a later scan can never answer from a stale file -- the scope IS the invalidation and no mtime or hash check exists. **This row's old "the one cost" claim was WRONG and is retracted here:** reads are 0.97 s of a 4.10 s wall (24%) while regex scanning is ~73%, so the honest ceiling of this fix is about a quarter of wall, not the 12x of iter 03 -- a later reader must not inherit the stronger claim. A second, non-performance reason the scope had to be one scan rather than none: two rules could previously observe two DIFFERENT versions of one file if a writer touched the tree mid-scan, and a live agent tree is exactly the target this verb is for. The 73% is row 35 |
| 30 | The consumer contract's stable-surface table is DERIVED from `build_parser()` instead of hand-copied | shipped | 5 of 7 rows named less than the CLI accepts: `report` omitted `--floor`, `prd` omitted `--project`, `scan` omitted `--json` (the exact object `docs/CONSUMER_CONTRACT.md` points a CI gate at), and `list` + `show` omitted their register-path positional, whose `nargs="?" default="."` makes a copied invocation read whatever register is in the consumer's cwd -- a silent wrong answer, not an error. Zero flags were invented, so the live drift was one-directional OMISSION and a one-sided "every documented flag exists" check would have passed the table unchanged |
| 31 | `radar scan --json` publishes the confidence floor it applied, and flags each finding against it | shipped | The one verb `docs/CONSUMER_CONTRACT.md` points a CI gate at published no floor at all, so a consumer had to hard-code the value 2 across a repo boundary where no test of ours can ever see it drift -- while the same document binds that gate to two rules the payload could not express (a `model-output` record may never block anything; a below-floor record carries its floor status). `_list_json` already published exactly this pair, so one product gave three floor stories: a flag on two verbs, a constant on `--prd`, silence on the machine surface. The contract's hand-copied key list was measured incomplete in the same run -- `target_name` and `findings` were emitted and undocumented -- so every emitted key is now asserted to appear in that paragraph, ONE-directionally: the reverse would fail on the deliberate negative statement that no blended `score` key exists |
| 32 | `render_scan` reaches the published `document` and `table` renderers instead of carrying private copies of them | shipped | `scan.py:250-252` was character-identical to `render.py:25-27`, the tail that implements the one-newline guarantee `VISION.md` names, while `diff.py:41` -- the newest renderer -- already imports `document`: so the docstring declaring `document` public *precisely* so no other module carries a second copy was false about its own repo, and `scan.py` was the outlier rather than the norm. Measured before the fix: the tail idiom occurred 2 times across the 11 `src/` modules, and `_table(...)` returned a list identical to scan's hand-built 7 table lines, so the collapse moved zero bytes. The new brake is a LEXICAL single-occurrence census over `src/*.py`, and it deliberately cannot see the second half: scan built its table from f-strings, so that was a SEMANTIC copy (all 3 `"| " + " | ".join` hits are inside `render.py`) and no occurrence count can catch a re-fork of it. That residue is unclaimed, named here rather than left for a later reader to assume the census covers it |
| 33 | The derived scores are reachable by a consumer that has not installed pydantic | shipped | Measured in iter 14 by scout A: `agent_gap_radar.taxonomy` imports stdlib-only, but `scoring` and `registry` raise `ModuleNotFoundError: pydantic`, so `agent-foundry` -- the one declared consumer (`docs/CONSUMER_CONTRACT.md:7-8`) -- orders records by the STORED integers instead. That lexicographic `(severity, frequency, tractability)` sort reads frequency before tractability while `priority` weights them 3/2/1, so over the live 16 records its top five is GAP-003, GAP-016, GAP-012, GAP-013, GAP-006 against our GAP-003, GAP-016, GAP-006, GAP-012, GAP-014 -- an inversion that is latent under the one config live today and live for any consumer taking the documented default. DECLINED in iter 14 on SHAPE: the fix is a three-way choice (a `TYPE_CHECKING` import plus a mapping adapter, a real `uv tool install`, or a committed derived index, the last declined once already as iter 07's B1), and an adapter adds a second public record shape that must not drift. Decide the packaging story in a doc BEFORE any code, and pin the inversion against SYNTHETIC records only -- pinning live ids is the iter-09 landmine SHIPPED in iter 73, and the three-way choice collapsed to its first option MINUS the adapter, because the adapter half was retired by measurement: `scoring` already reads records duck-typed (`_source_key` is total over any object carrying `source_class`), so raw JSON wrapped in `types.SimpleNamespace` scores identically -- 80 identical / 0 different over 16 records x 5 functions with pydantic blocked in-process, and `rank` / `below_floor` id orders unchanged. Two edits, one file: the `models` import moved under `TYPE_CHECKING`, and the module's one pydantic-only call (a record clone inside `_confidence_with`) became `_EvidenceOnly`, a frozen dataclass carrying only the `evidence` field `confidence()` reads -- REQUIRED, not tidying, since moving the import alone leaves `promotion_options` raising `AttributeError` on exactly the object shape this row invites a consumer to pass, and that is the one function whose whole job is telling a below-floor record how to earn promotion. Behaviour-neutral on the pydantic path (16/16 identical against the pre-change module, all 16 prescriptions non-empty, so the swapped line is exercised on every record) and byte-neutral on all 8 verbs. `docs/CONSUMER_CONTRACT.md` publishes which modules import without pydantic -- DERIVED from the same in-process blocker the suite runs, so a module that later grows a runtime pydantic import reds the document -- plus the duck-typed attribute set `scoring` requires. Row 39 and the consumer's own side stay open: the honest claim is that the module is reachable, never that the consumer agrees with us |
| 34 | The scan document's `target` field is portable across machines | shipped | Iteration 86, the only board row that ALTERS committed bytes rather than adding to them. `scan()` resolved the target (`.expanduser().resolve()`) and emitted that absolute path on BOTH surfaces -- the markdown `Target:` line and `scan --json`'s `"target"` -- so `radar scan .` answered with the scanning machine's own root, account name included, and the one artifact `docs/CONSUMER_CONTRACT.md` tells a CI gate to commit and diff was reproducible only on the machine that produced it: the same committed invocation reports a change that did not happen. `target_name` already published the portable identity, so the fix removes an INVENTED value, not information. NOT a quality-bar violation as the original slate framed it -- that bar forbids absolute machine paths in code, tests and gap records, not in runtime output. This row demanded its three-way choice be picked explicitly, and it is DECIDED: **echo the caller's spelling verbatim**, with no expansion, no resolution and no trailing-slash normalisation, while `target_name` and the `# Gap scan:` heading keep deriving from the RESOLVED path so the document still states WHAT was scanned. The other two ways are rejected on record: relativising against the cwd makes the field a derived value the caller never typed and can emit `../..` chains or fail across volumes, and keeping the root out of the document entirely would delete a published key from a payload a gate already parses. The stderr diagnostic deliberately keeps the resolved path, because a path that does not exist is most usefully named in full and stderr is not the document. Byte-equality therefore holds per INVOCATION, not per target -- two spellings of one directory still differ in that one field, and a behavior pins that they differ in NOTHING else |
| 35 | Memoise the `(pattern, file, exclude_tests)` decision, not just the file content | open | The 73% row 29 cannot reach. Measured in iter 15 by scout B: 4,428 rule evaluations resolve to 3,558 distinct triples, so 870 (19.6%) are exact duplicate re-evaluations, because 49 content rule nodes carry only 33 distinct patterns; a pair-keyed memo skips the read AND the regex. Split from 29 deliberately on RISK, not on size: a content cache keyed on a path can only ever cost a performance MISS, while a decision memo whose key omits any input that changes an outcome returns a WRONG verdict silently, in a tool whose entire value is that its verdicts are honest. Enumerate every input `evaluate` reads for a content rule and prove the key covers them BEFORE caching a decision. RE-MEASURED at 120 records in iteration 93 and the premise is REDUCED, not refuted, so this row stays `open` on the RISK sentence above rather than on its size. Row 35's OWN metric -- `(pattern, file, exclude_tests)` evaluations over this repo -- is 54,504 evaluations resolving to 47,524 distinct triples, so 6,980 (12.8%) are exact duplicate re-evaluations against the 19.6% measured at 16 records, and the stated CAUSE barely moved: 506 content rule nodes carry 361 distinct patterns, 28.7% duplicate-carrying against 32.7%. Recorded because iteration 93's spec reported this premise REFUTED at 2.9% and that figure is a DIFFERENT metric measured over a DIFFERENT domain: 2.9% is 11 duplicate `(kind, pattern, globs)` NODE triples of 377 nodes, and those 377 are `present_when` plus `mitigated_when` only, omitting `applies_when`; over all three buckets the same node metric is 76 of 506 (15.0%). A node-triple count cannot refute a row about evaluation pairs, because the pairs are what the memo would skip -- so the 12.8% is the figure a later iteration should price this row from, and it is a smaller prize than 19.6%, not an absent one |
| 36 | `docs/CONSUMER_CONTRACT.md` promised a ninth verb, `radar ingest`, that has no parser entry, no roadmap row and no code | shipped | The contract's own heading vocabulary made the drift decidable: `(SHIPPED)` on the `radar scan` heading against `(TO BUILD)` on the `radar ingest` one, while iter 10 derived the stable-surface TABLE from `build_parser()` and left the HEADINGS unchecked. DECIDED: NOT PLANNED, and not built. The job that section described -- read a loop's own corpus, propose DRAFT records -- is already done OUTSIDE the byte-stable CLI by `tools/promote.py` plus `research/CANDIDATE_CONTRACT.md`, and that gate enforces the section's own value MORE strictly than the section proposed: it REFUSES a candidate whose evidence is entirely zero-weight source classes, whose locator is not a fetchable URL, whose quote runs under six words, or which carries no check at all, instead of admitting an unciteable record below the floor and trusting that a human returns for it. The section's ARGUMENT is kept as a design note rather than deleted, because `VISION.md` names that invariant as the one it protects. Second nomination, NOT a new finding: raised as iter 10's candidate B3 and recorded at `DIRECTIONS.md:61` -- two repos agree on the verb and neither roadmap has a row for it -- where it lost triage to the stable-surface table. The brake is LEXICAL over `##` headings and one-directional by construction: a section that describes a verb without naming it in a `radar <verb>` heading escapes it entirely, and it cannot see the MIRROR promise in the consumer's own tree at `agent-foundry/docs/INTEGRATION_AGENT_GAP_RADAR.md:192`, which is another repo and outside every test this suite can run |
| 37 | GAP-014's `present_when` pattern is 39% of every scan's regex time, and 7.4x of that is provably free | shipped | Measured in iter 16 by scout A over `agent-foundry` (231 tracked files, 4.3 MB of distinct scannable text): the 24 evaluated patterns span 3.0 MB/s to 3,003 MB/s, a 1,000x range, and GAP-014's leading unanchored `[\w/.-]*` in front of an alternation makes the engine retry and backtrack at every offset -- 1.304 s, 39.3% of all regex time, 174 files scanned to return ONE hit file. Deleting ONLY that prefix measured 1.261 s -> 0.171 s on the same corpus with the matched file set and every reported line number identical, and the equivalence is structurally arguable rather than lucky: the prefix is unanchored, so it cannot change WHETHER the regex matches, and `[\w/.-]` cannot match a newline, so it cannot move a match's start across a line boundary, which is all a `path:line` location carries. NOT taken in iter 16 for two reasons a later PM should weigh rather than inherit. (a) It edits `gaps/`, which is another writer's in-flight data -- the recurring research pass promotes into that directory and row 27 already records an iteration deliberately leaving it alone for exactly this reason; `tools/promote.py` only ADDS records with gate-assigned ids, so a concurrent promote cannot touch this field, but that is a fact about today's gate and not a guarantee. (b) The proof is byte-equality on two corpora, not equality in general: a greedy leading `[\w/.-]*` can MERGE two adjacent hits into one match, so the thing to assert is the location COUNT and the rendered bytes over at least two committed fixture targets, in the same commit that changes the field. Fold in the free line-number fix while there -- counting newlines up to the match offset instead of copying a prefix slice removes 12.4 MB of string copies per scan, is provably byte-identical, and is worth 0.2% of wall, too small for its own iteration. **SHIPPED in iteration 27**, re-measured there against THIS repo as the target (53 files matching the rule's globs, 825,990 bytes, python 3.13.0) rather than `agent-foundry`, so these figures sit beside the iter-16 ones above and do not replace them: the pattern is 304.8 ms of 1,042.3 ms (29.2%) of the register's whole content-regex budget over all 49 content patterns -- the largest single item by 3.3x -- and 304.8 ms -> 35.4 ms on the same files, 8.0x. End-to-end `radar scan .` median wall 0.73 s -> 0.49 s (-33%), and the emitted document is byte-identical at 5,270 bytes. The superlinear axis is LINE LENGTH and it is the real prize: 40 lines of 3,200 class characters cost 8,766 ms before and 5.06 ms after (1,733x), and `**/*.js` is in this record's own globs, so one minified bundle made the verb look hung. Objection (b) above is ANSWERED rather than assumed: a greedy prefix can indeed merge two adjacent hits into one match, but `evaluate` breaks after the FIRST match in each file, so exactly one `path:line` is reported per matching file either way and a merge is unobservable -- and the byte-identical document is the independent control the row asked for. Objection (a) still stands as written and was accepted: `tools/promote.py` only ADDS records under gate-assigned ids, so it cannot touch this field. Row 37's suggested fold-in (counting newlines instead of slicing a prefix) was deliberately NOT taken -- it is a `src/` change worth 0.2% of wall, and this iteration's payload is register data only; it is recorded as row 63 so it cannot be lost. |
| 38 | `research/CANDIDATE_CONTRACT.md` documents the `not` combinator as taking `rules`; the loader reads `rule`, so the documented shape is REJECTED at load | shipped | Measured in iter 17 by scout A through the real loader, both directions: the documented `{"kind": "not", "rules": [...]}` is refused ("not requires a 'rule' object") and `{"kind": "not", "rule": {...}}` is accepted. Two independent sites read `rule` (`models._validate_rule`, `checks.evaluate:443`), and the register itself disproves the document -- GAP-012 and GAP-013 each carry a `not` node keyed `rule`. This file is the ENTIRE spec handed to context-free unattended research agents, so a candidate that follows it dies at schema load with a message contradicting the document it was told to obey, and the agent cannot tell a spec bug from its own mistake. Lost iter 17's triage on two grounds a later planner should weigh rather than inherit: the observed cost so far is ZERO (the only two live `not` nodes already use the right key), and its increment shape is iteration 16's shape exactly, which the repetition brake exists to interrupt. Iter 16's numeral census over this same file passed the "7-of-7 rule kinds" claim and was RIGHT -- every documented kind NAME exists -- so a one-sided name check is structurally blind to a wrong KEY. The durable brake is a ROUND-TRIP, not a proofread: pull the fenced JSON skeleton out of the markdown, push it through the real loader, and assert for each combinator in `RULE_KINDS` that the key the document names is the key `_validate_rule` reads. Do NOT fold in this file's 6 em dashes (the only tracked doc carrying any) -- that turns a one-word correction into a whitespace diff that hides the fix SHIPPED in iteration 28, and the two iter-17 objections are answered rather than overruled: the ZERO-observed-cost point stands for the register (both live `not` nodes already use `rule`, and this iteration changes no record) but not for the DOOR, which is the surface the row is about -- an unattended author writing the documented shape is refused at `Gap.model_validate`, the same call `tools/promote.py:167` makes, and cannot tell a spec bug from its own error. On shape, the increment is no longer iteration 16's: 16 counted NAMES in prose, this adds the first fenced-JSON round-trip in the suite and asserts the extracted kind set EQUAL to `models.RULE_KINDS` (referenced by zero tests before now), so a kind added to the loader reds the document until it is exemplified. Re-measured before speccing: `not`+`rules` REJECTED, `not`+`rule` ACCEPTED, `any_of`/`all_of`+`rules` ACCEPTED, `any_of`+`rule` REJECTED; live kind census `content_matches` 44, `all_of` 10, `content_absent` 5, `file_exists` 4, `any_of` 3, `not` 2, `file_absent` 1. `checks.evaluate:443` from this note has MOVED to `checks.py:500` -- a line number in a note is a derived value. DELIBERATELY NOT ASSERTED: the corrected prose sentence itself, because a wording matcher reds a correct suite on a legitimate rewording; the fenced example is the machine-checked normative statement, and that trade is recorded here so a successor does not read it as an oversight |
| 39 | The consumer contract's honesty rule says a cited gap must be `open`, while the register's #3 record is `partially-addressed` | shipped | SETTLED in iteration 100: `partially-addressed` IS citable, published as an EXCLUDE-list (`taxonomy.TERMINAL_STATUSES` enumerates `addressed` and `retired`; `citable_statuses()` derives the rest from `STATUSES` at call time), because doneness needs POSITIVE evidence and a status the register later adds must not silently leave the build pipeline. The premise below had DECAYED and is corrected here rather than restated: at 120 records GAP-016 is #3 (not #2) and GAP-012 is #25 of 120 (not #4), so ONE top-four record is `partially-addressed`, not two, and the honesty rule now lives at `docs/CONSUMER_CONTRACT.md:403`, not `:143`. Original measurement, kept: Measured in iter 17 by scout A: `docs/CONSUMER_CONTRACT.md:143` publishes "Gate on honesty (a cited gap really exists, is open, and is above the floor)", `taxonomy.STATUSES` holds FOUR values, and GAP-016 (priority 9.0, second row of `radar list`) and GAP-012 (8.7, fourth) are `partially-addressed` -- so a gate implementing the published rule literally REFUSES a truthful citation of the joint-highest-priority record. Fail-CLOSED, and the tool disagrees with its own document: nothing in `src/` filters on `status`. The reads are all DISPLAY or validation -- `render.py:110`, `cli.py:95`, the `diff.py:48` field list, the `models.py:213` validator -- so `radar prd` and `scan --prd` will build against a `partially-addressed` record today while the contract tells the gate to reject the citation. NOT a wording fix: it needs a PRODUCT DECISION first, and it is the lead's to make, because `partially-addressed` means mitigations exist in the wild, which is arguably what a build loop should not claim credit for closing. A confident wrong sentence here is worse than today's sloppy one. If the decision is that such a record stays citable, the honest restatement is "not `addressed` and not `retired`", asserted against `taxonomy.STATUSES` alone and NEVER against the statuses present in `gaps/` (one `retired` record from the research pass would red a correct suite days later). Distinct from the two earlier nominations at `DIRECTIONS.md:101` and `DIRECTIONS.md:107`, which both proposed CODE and both lost |
| 40 | `confidence()`'s corroboration point counts class LABELS, so one source cited twice under two labels lifts a record with no new evidence | shipped | `scoring.py:55-57` built `{e.source_class for e in gap.evidence}` and added the point when that set reached two, while the function's own docstring promised "two or more INDEPENDENT source classes" -- nothing checked that the two labels came from two SOURCES. Measured through the real `confidence()` in iter 18: GAP-009's single citation labelled `practitioner-report` scores 3, and the SAME locator entered twice as `practitioner-report` plus `secondary-summary` scores 4 -- one source, no new evidence, one extra point, and that point is what moves a record across the floor. This is the one rule `VISION.md` names as the rule the register exists to protect, and the exploit needs no malice: 4 of the 16 live records already cite fewer distinct sources than citations (GAP-015's three citations are ONE postmortem, GAP-014 five to two, GAP-011 four to three, GAP-016 four to three), all labelling their repeats with the same class today, and `tools/promote.py::_gate_evidence` -- which wrote GAP-014 and GAP-015 -- checks that the locator is a fetchable URL and never that it is DISTINCT. Taken while the cost is ZERO: 0 of 16 confidences move and every rendered surface is byte-identical, where the same fix after drift DROPS a published confidence and becomes a contested ranking change. Source identity is the locator with a `#fragment` removed, a trailing `/` removed and case folded; two documents at one DOI host, or two paths in one repo, stay DISTINCT sources deliberately, because merging them would withhold an honest point. The change is monotone DOWNWARD -- it can only withhold a point, never grant one -- which is why it cannot inflate anything. The coupling worth remembering: `_ClassOnly`, the probe citation `promotion_options()` appends, raised `AttributeError` the moment `confidence()` read a locator, so it carries a synthetic locator that reads as a NEW source, which is what keeps the below-floor `Needs` column unchanged |
| 41 | `radar show` renders six sections and none of them says whether the gap is DETECTABLE, so a manual-only record is indistinguishable from an automated one | shipped | Measured in iter 18 by scout A: `render.gap_brief`'s docstring calls itself "everything a builder needs to act on it" and renders Problem, Symptom, Why this is still open, Existing partial solutions, Build hypothesis and Evidence -- nothing from `gap.check`. Over the live 16, 15 carry a check, 12 are automated, 3 are manual-only (GAP-001, GAP-002, GAP-005) and GAP-010 has none, so the four records `radar scan` can never return PRESENT for read exactly like the twelve it can, in the view a human reads before starting work. `scan.py:212` already prints "Gaps with no check yet: N", but only in a scan of a target, so the fact is reachable only by someone who already has a repo to scan. NOT taken in iter 18 on SHAPE alone: the repetition brake reported `verdict=RUT` and the candidate's own nomination text names it "the same shape as iteration 17's layer table", on the surface iter 17 had just moved. Render the DECLARATION and never a judgement -- `checks.py:505` already supplies the neutral words -- and land it BEFORE row 8, or the golden baselines a brief that hides the check |
| 42 | The report prints a citation COUNT, and independence rests on the source count it does not print | shipped | Split from row 40 deliberately and on RISK, not size: 40 corrected the derived score and moved zero rendered bytes, while publishing a per-record distinct-source count is a byte change to a committed human surface and belongs behind row 8's goldens. What it would add: nothing on any surface today lets a reader see that GAP-015's three citations are one postmortem, or that GAP-014's five are two sources -- the count that a corroboration rule actually keys on is the one the register never publishes. SHIPPED in iteration 65 as one derived line under `radar show`'s `## Evidence`: `<N> citation(s) across <M> distinct source document(s). Independence is counted by document, not by citation.`, keyed on `scoring._source_key` -- the same function `confidence()` uses -- so the published count cannot disagree with the corroboration rule the scorer enforces, the discipline `diff.py` already follows by CALLING `scoring` instead of re-deriving. Measured at ship over the live 16 records: 41 citations rest on 26 distinct source documents, and 4 of 16 hold more citations than sources (GAP-011 4->3, GAP-014 5->2, GAP-015 3->1, GAP-016 4->3); all four sit at confidence 5, the CEILING, so the corroboration point `confidence()` already withholds cannot move the published number and nothing distinguished five independent sources from five excerpts of two. Row 8's goldens objection was answered with iteration 29's discipline rather than by waiting on it: byte-neutral outside the new line on 16/16 live records. The count feeds NO score and enters NO ordering, and its trailing sentence is constant, so the line publishes a denominator and never a verdict -- a source count read as a target is the Goodhart shape `docs/CONSUMER_CONTRACT.md` forbids by name. TITLE CORRECTION, measured this iteration and not restated: the title names `radar report`, which prints NO citation count at all (0 hits for `citation` in its stdout, against a control of 2 for `Priority`) -- the only site publishing one is `diff.py:122`. The row's substance survives and strengthens, since BOTH counts are absent from the ranked surface; the title is left as written rather than rewritten after the fact. Adding a 10th `sources` field to `radar diff`'s CLOSED nine-field set is deliberately NOT this row |
| 43 | `checks._TRACKED_CACHE` is process-lifetime, in the one module that argues against process-lifetime caches | open | Measured in iter 19 by scout A: `read_cache_scope`'s docstring says a process-lifetime cache "buys the opposite defect" and that "the SCOPE is the invalidation", while `_TRACKED_CACHE` is a module global with neither. Reproduced in one process: scan 1 misses, a writer adds a tracked file containing the pattern, scan 2 answers False from scan 1's file LIST, a manual `.clear()` then returns True. The CLI is immune (one scan per process) so this is latent, reachable by a library consumer and by the suite -- where 4 tests reach into the private global and call `.clear()` 8 times, and nothing makes the NEXT git-based test remember to. The memo must MOVE into the existing scope frame, not go: one real scan is 32 calls / 31 hits, i.e. 31 `git ls-files` subprocesses avoided. The lead must first choose what `run_check` outside a scope does, because uncached the fixture harness goes from 24 spawns to 108. RE-MEASURED in iteration 31 on one real `radar scan .` over the live 16 records: 34 `tracked_files()` calls, 33 hits, 1 `git ls-files` subprocess -- this row's 32/31 figures had drifted and are corrected here |
| 44 | `Evidence.locator` has no validator, so a citation nobody can check passes `radar validate` | shipped | Measured in iter 19 by scout A: the quality bar names a locator AND a verbatim quote in one sentence; `models.py` validates the quote and nothing validates the locator, so `""`, whitespace, and "not a url at all" are all ACCEPTED and `validate` then certifies the register. Three doors disagree: the schema requires nothing, `tools/promote.py` requires `https?://`, `check_locators.py` SKIPs a non-URL and still exits 0, `verify_quotes.py` flags it. Iter 18 made the field load-bearing for a DERIVED score -- `_source_key` normalises the locator, so two citations with empty locators are ONE source and the corroboration point is silently withheld (5 with distinct locators, 4 with both emptied). Live cost is ZERO today (all 16 records' locators start with `http`). The lead must choose the RULE: non-empty-and-not-blank is consistent with the field's own docstring, while the promote gate's URL rule would reject a bare DOI the schema currently promises to accept |
| 45 | The on-disk record file is the ONLY surface a live consumer reads, and no document covers it | shipped | Measured in iter 20 by scout A across both repos: the one declared consumer (`docs/CONSUMER_CONTRACT.md:7-8`) consumes none of the eight verbs, no JSON payload and no exit code -- it reads `gaps/*.json` as plain local JSON on purpose -- while that document contains the string `gaps/` ZERO times. It hard-depends on six stored keys (`id`, `status`, `layer`, `severity`, `frequency`, `tractability`) and renders `title`, `gap_type` and `evidence[].source_class` through `.get(key, "")`, so a rename of those three reaches its PM prompt as EMPTY STRINGS and a rename of the six shows up only as an `unreadable` count. Nothing raises on either side of the boundary. DECIDED in iter 20: the record file IS a supported read-only surface (the register is files in git, which this roadmap's own non-goals call the feature), the documented key NAMES are not renamed or removed without a ledger row, and new fields may be added at any time |
| 46 | The `taxonomy` verb carries half a copy of the published one-newline tail, and the row-32 census reports it ABSENT | shipped | Measured in iter 21 by scout A and re-measured by the planning role before speccing: the argument-agnostic idiom -- join a list on a newline and append exactly one -- occurs 2 times under `src/agent_gap_radar/` (`render.py:27`, `cli.py:183`) across 11 files, while `test_one_newline_tail_has_exactly_one_implementation` PASSES and reports `{'render.py': 1}`, because iteration 14's `TAIL_IDIOM` requires the pop loop ADJACENT to the join tail and `cli.py` copied only the join half. `document(out)` is byte-identical today (1894 bytes both ways) purely because the taxonomy list ends on a bullet: append one section in the file's own style and the current tail emits a blank line before the final newline while `document()` absorbs it. So a guarantee the quality bar names by name holds on the one verb with no golden fixture by accident of list order. A census that matches the BODY reports a partial copy absent, which makes a well-covered repo read as healthier than an untested one -- the inverted signal this product already fixed in `mitigated_when`. Free rider in the same file, first raised in iter 03 and still live 18 iterations later: `cli.py` imports `confidence` and `priority` and loads neither (AST Name-load count 0), and it is the ONLY module under `src/agent_gap_radar/` with an unused import. |
| 47 | `content_absent` keeps scanning after its boolean is decided, and a boolean-only context does the same | open | Measured in iter 21 by scout B over `agent-foundry` (231 tracked files, 4.32 MB of distinct decoded text, the same target iters 15/16 used so the figures are comparable): `radar scan` end-to-end CLI wall is 3.24-3.39 s of which the content regex is ~91%. `checks.py:476-477` returns `RuleHit(not found, [])` -- locations are DISCARDED once the pattern is found -- yet the loop scans every remaining file, and 90-98% of files are scanned after the boolean was already decided, costing 464.5 ms (16% of content-regex time, ~14% of scan wall) for one guarded `break`. The same shape in a boolean-only context (`applies_when`, `not`; `checks.py:493` reads only `__bool__`, `checks.py:446` returns `[]`) is a further 404.0 ms, 868.4 ms combined. Recorded as ONE row on purpose and to be shipped in two bites: the second half is the `exclude_tests` propagation shape, which already produced a fail-open here, so it must not ride along with the first. This lost iteration 21's triage on privilege, not on merit -- there was no `SPEED_STORY_NEEDED.md` flag -- and it is the recommended next pick.. BITE 1 SHIPPED in iteration 94 and bite 2 REMAINS, which is why this row stays `open`: the boolean-only / `exclude_tests` propagation shape has already produced a fail-open in this module, so it must not ride a control-flow change. RE-MEASURED at 120 records against THIS repo by iteration 94's scout A, and the earlier `agent-foundry` figures above are kept as the record of what iteration 21 measured rather than as a live estimate: `scan()` median wall 16,517 ms, content regex 96.2% of it over 28,502 `(pattern, file)` passes, and 3,767 pairs scanned after a `content_absent` boolean was decided cost 3,321.3 ms = 20.1% of wall -- a LARGER share than the ~14% measured on `agent-foundry`, over only 28 `content_absent` evaluations, which is what makes the ratio lopsided and also what makes it sensitive to what the next research pass promotes |
| 48 | `tools/promote.py` skips its summary line on a vacuous run, so the one door the register is fed through cannot report that it examined nothing | shipped | Measured BY EXECUTION in iter 22, not read off the source: an empty inbox prints exactly one line, `No candidates in <inbox>`, and exits 0, because `promote()` early-returns before the `N accepted, M rejected` summary. That breaks a promise the live research driver depends on BY NAME -- its own comment says the tool prints a summary line on every successful run, so its absence means the tool itself died, and it reports `promote CRASHED` whenever the string `accepted` is absent from stdout, so a HEALTHY empty-inbox run reports as a crash. The direction is the OPPOSITE of the scout's reading (it argued a vacuous run reads as success); one cause either way, which is that the census is CONDITIONAL. Fix is row 22's and row 24's idiom applied to the ingest door: print `examined N candidates in <inbox>` on every run and let the vacuous case FALL THROUGH to the single existing summary emitter instead of adding a second copy. Exit codes deliberately unchanged -- the consumer that reads them lives outside this repo, where no test here can see a break, so printing a line adds a signal while changing a code changes a contract this loop cannot verify. |
| 49 | `README.md`'s confidence rule names only the CLASS dimension, so the front door still describes the pre-iteration-18 behaviour | shipped | Measured in iter 22 by scout B and re-verified from both artifacts before speccing: `README.md:11` says two independent classes add a point and never mentions the source DOCUMENT -- the exact vocabulary the pre-fix code used (two or more INDEPENDENT source classes), so it cannot distinguish the case iteration 18 closed -- while `docs/CONSUMER_CONTRACT.md:93` states the real rule and `scoring.confidence` implements it pairwise. Measured through the real `confidence()`: two classes on ONE locator scores 3, on TWO locators scores 4. Live cost TODAY is zero, since both sides sit above the default floor of 2 and no record's floor status moves, which is the same zero-cost-today ground that lost rows 38 and 44 their triage. The counter a lead should weigh: this is the only place a human learns the rule `VISION.md` names as the one the register exists to protect, and the register is fed by unattended agents that read documents as specification. One sentence plus a two-sided synthetic truth table.. STATUS CORRECTED in iteration 94, not built: fixed in iteration 64 by commit `c591b7e` with a Done-ledger bullet, and enforced since by the committed brake `tests/test_readme_confidence_rule.py` through the real `scoring.confidence()`. Re-verified from the artifact rather than from the slate: `README.md:11` now reads "two citations differing in both class and source document add a point (two labels on one URL earn nothing)", so the row's stated defect is refuted by the document AND by a test |
| 50 | The research contract's fenced self-verify command is not round-tripped against `promote.py`'s parser | open | Measured in iter 22 by scout B: every flag the fenced command at `research/CANDIDATE_CONTRACT.md:127` names (`--inbox`, `--gaps`) exists today and dry-run IS the default, so this starts GREEN and is a BRAKE rather than a fix -- which is exactly why iteration 22 left it OUT of its own bite instead of bundling a green assertion into a red one. Sibling of row 38, which round-trips the fenced JSON SKELETON through the real loader and is still live and wrong (`rules` versus `rule`); neither subsumes the other, and a lead may reasonably land both over that one file in a single iteration. |
| 51 | Reject a file before the regex runs, using the literals the pattern itself requires | shipped | Measured in iter 22 by scout A over `agent-foundry` (231 tracked files, 4.32 MB of distinct text): the tool spends 88.2% of its regex budget (3.158 s of 3.582 s, over 5,156 of 6,342 pairs) proving a pattern is ABSENT, and no open performance row attacks that -- 35, 37 and 47 all attack matching-side or duplicated work. 30 of 33 distinct patterns are a top-level alternation of literal-ish alternatives, so a match REQUIRES one of a small literal set; extracting it and testing against a once-per-scan lowercased copy rejected 3,829 of 6,339 covered pairs (60.4%) for a net 1.033 s -- 29.7% of regex time, about 32% of scan wall -- with 0 unsound rejections, checked on every covered pair in both directions. REFUTED on the way there and recorded so it is not retried: the textbook SINGLE mandatory literal covers only 3 of 33 patterns and measured -0.002 s, a LOSS. Held back deliberately in iter 22: an unsound literal turns a PRESENT into an ABSENT, the inverted signal `VISION.md` calls a regression regardless of how much cleaner the output looks, so this needs a strictly DORMANT first bite (the extraction function plus a differential test over the live register asserting identical verdicts) before any call site moves, and it should wait for a `SPEED_STORY_NEEDED.md` flag.. BITE 1 SHIPPED in iteration 93 and bite 2, the call site, REMAINS, which is why this row stays `open`: `checks.required_literals(pattern)` returns the lowercase literal set every match of a content pattern must contain, or `None` when it cannot prove one, and NOTHING calls it. Fail-CLOSED by construction -- every ambiguity returns `None`, which costs one regex pass, where a wrong set skips a file and turns a PRESENT into an ABSENT. Measured at 120 records over all three rule buckets: 361 distinct content patterns, 245 provable and 116 refused, 0 empty sets and 0 raises, with 0 unsound rejections over the 11 tracked `src/agent_gap_radar/` files. No speedup is claimed or measured here and 245-of-361 is a coverage figure, not a performance one: a dormant function cannot make a scan faster, and bite 2 is the change that has to re-prove every scan verdict. BITE 2 SHIPPED in iteration 99 and this row is CLOSED: `evaluate`'s content branch asks the extractor for its pattern's literal set once per rule evaluation and skips the regex on any read file whose folded text holds no member of that set, with the fold memoised for exactly the lifetime of `read_cache_scope()` so a file is folded once per scan rather than once per rule. The read still happens, so no file scanned today becomes unscanned and the truncation accounting above it is untouched. Shipped on a DIFFERENTIAL gate rather than on the regex-dialect reasoning this row warned against |
| 52 | The stable-surface table notates a defaulted positional two different ways, and publishes none of the six real defaults | open | Measured in iter 22 by scout B against `build_parser()`: six arguments carry a default (the `path` positional on `validate`, `list`, `report`, `show` and `prd`, all `nargs=?` with `default=.`, plus `scan --gaps`), yet `radar list [<repo>]` brackets it while `radar validate <repo>` does not, and `radar prd <repo> --gap <ID>` reads as a required flag when `--gap` is optional. The axis cannot drift-red: `tests/_surface_contract.py:308` strips the brackets before comparison and `VerbSurface` carries no requiredness and no default. All three misnotations point in the SAFE direction (a consumer passes more than it needs), so unlike row 30 there is no measured victim. Publishing the defaults needs an EXPLICIT choice -- a new `Default` column read by name, or a second reader -- because widening the cell-0 rule would weaken a brake that deliberately does not read the Promise cell (`_surface_contract.py:26-30`), and that choice should not be made in passing. |
| 53 | `radar prd` declares whether a gap is statically DETECTABLE, and names where the register's own two-sided reproduction sample lives | shipped | `prd.py` emitted `sourceGap` with six keys and no `check`, so the one artifact the schema REQUIRES an automated record to hold -- a bad/good sample proving the check discriminates -- was dropped exactly at the hand-off, and story US-001 ("Reproduce GAP-XXX as a failing test") was emitted verbatim for the 4 of 16 records the register itself declares undetectable. The payload now carries a `check` object with `detectability` plus, for an automated record, a `recordGlob` POINTER (`gaps/<ID>-*.json`) rather than the fixture bytes. POINTER not payload was decided on measurement: inlining costs +5,456 B (+141%) on GAP-016, whose fixtures alone are 2,605 B, against roughly +700 B uniformly for the pointer, and a PRD enters a build loop's prompt while this register's own GAP-005 cites a growing required-reading file killing a loop on a step cap. `Check.is_automated` is the single copy of the automated/manual predicate, read by both `models` and `prd`, so rows 32 and 46's duplicated-rule defect is not re-introduced. The glob is a glob and not a filename because records are stored `gaps/<ID>-<slug>.json` and the two `_slug` implementations truncate at 48 and 56 chars, so a derived filename resolved for 0 of 12 automated records |
| 54 | An opt-in `radar prd --with-fixtures` inlines the reproduction sample bytes | open | Split from row 53 on measured size, and DECIDED rather than deferred there: the default stays a pointer. Only worth building for a consumer that cannot read the register's own files, which the one declared consumer can -- iteration 20 made the on-disk record file a documented read-only surface, which is what makes the pointer legitimate |
| 55 | A derived brake asserting every emitted `prd.json` key appears in a document | shipped | Iteration 68, landed WITH the rename in row 71 rather than after it, because a published key set is what stops that fix decaying: `docs/CONSUMER_CONTRACT.md` now names the five emitted top-level keys and the brake DERIVES the expected set from the emitter's own bytes at test time, two-sided over in-memory documents, so a sixth key reds the suite until the document names it. Its heading deliberately does not spell a `radar` verb inside a code span, so iteration 16's verb-heading rule and the deeper-heading vacuity check stay green |
| 56 | `radar report` gains a shared-source view, so a reader can see that several records rest on ONE source | shipped | Runner-up on iteration 23's slate. Measured there: keying on the normalised TITLE finds nothing (max pairwise Jaccard 0.095 with stopwords removed, so a title-token duplicate detector has no known-bad sample in this register and row 11 is mis-keyed), while keying on the LOCATOR finds 7 real overlaps and one source carrying 3 records. It moves committed report bytes while the goldens row 8 is still open, so it should not go before them |
| 57 | The three locator doors disagree on what a locator must LOOK like, so hardening past non-blank makes two of them wrong | open | Split from row 44 in iteration 24 and deferred DELIBERATELY, with the measurement recorded so a successor does not re-derive it: `models.Evidence` now rejects only a blank or whitespace-only locator, `tools/promote.py:79` requires `https?://\S+$`, and `tools/check_locators.py:58` SKIPs any non-`http` locator and still exits 0. The field's own docstring promises "URL, DOI, or a stable local artifact path", so promote's URL rule would REJECT the bare DOI the schema advertises, while check_locators silently passes a local path it never resolves. Non-blank is the ONLY rule consistent with all three doors, which is why iteration 24 stopped exactly there; anything stronger is a NEW product promise about what a locator IS, and it must first name which of the three doors is authoritative. Live cost today is zero -- all 41 citations begin `https://`, so every door agrees on every live record and the disagreement is reachable only by new data. That is an argument for speccing it before drift, not for bundling it into the bite that closed the blank hole. |
| 58 | A departing reader decides the CLI's exit code, so the published error contract is false in two ways no document mentions | shipped | Measured in iteration 25, control-first: a closed reader -> exit 120 plus `Exception ignored on flushing sys.stdout`; a document past the 64 KiB pipe buffer piped to `head -2` -> exit 1 plus a raw traceback. One guard in `main` returns 141 (the shell's 128+SIGPIPE) with an empty stderr, and `1` is left unused on purpose so row 25's floor-gated verdict code has a free value |
| 59 | A non-git scan borrows `Path.glob`'s dialect, so a schema-valid register can crash it or change its verdict | shipped | Measured in iteration 26 on 3.13.0, same register and same file bytes on two targets differing only by `.git`: an absolute glob (`/etc/**`) -> non-git rc **1**, 0 stdout bytes and an uncaught `NotImplementedError` traceback vs git rc 0 and a 1396-byte document; an empty-string glob -> non-git `UNKNOWN -- Unacceptable pattern: PosixPath('.')` vs git `PRESENT`. `_glob_regex`'s own docstring already names the 3.12/3.13 trailing-`/**` divergence it was written to fix, and `checks.py:212` already footnotes that a non-`ValueError` escapes to the top. One matcher swap in `_iter_walked` retires the borrowed dialect |
| 60 | `_iter_walked` has no symlink-containment check, so a non-git target can quote content the target does not hold | shipped | Split from row 59 in iteration 26 and deferred DELIBERATELY. `_iter_tracked` rejects a match whose `resolve()` leaves the target (added when the tracked path stopped walking); the walked path never had it, so a symlinked file in a non-git tree is scanned and its lines are quoted as evidence about a tree that does not contain them. A containment rule is a different question from a matching rule, which is why row 59 stopped at matching. SHIPPED by iteration 31, which re-landed iteration 30's payload unchanged rather than rebuilding it: the rule is the same `resolve()`-leaves-the-target test `_iter_tracked` already applies, placed in the MATCH loop rather than at descent because measurement reversed the cheap argument -- one instrumented scan of a non-git copy enters `_iter_walked` 34 times, so descent placement costs 34 x (files walked) `resolve()` calls against roughly 1,826 in the match loop. Two agents measured the walked-file denominator as 83 and the tracked-file count as 84, so the comment's figure is left as written rather than 'corrected' to an unsettled number; the 34 is confirmed exactly |
| 61 | `_validate_rule` accepts any string as a glob, so `""` and `/etc/**` are schema-valid | open | Measured in iteration 26: `models.py:188-190` requires a non-empty globs LIST and never inspects an element, which is what let both crash probes into a valid register. Iteration 78 shipped ONE limb of this row and DECIDED AGAINST the rest, so the title stays live BY DECISION rather than by omission. Shipped: a `globs` element must be a `str`, the one shape the matcher cannot answer at all -- `globs=[123]` passed `radar validate` at rc=0 with 27 bytes of stdout, then made `radar scan` exit 1 with an `AttributeError` traceback and ZERO document bytes, which is unreachable from `_glob_regex` because the crash precedes the regex and `AttributeError` is not a `ValueError`. DECIDED AGAINST, with two measurements behind it rather than a preference: the SHAPE limbs (blank, absolute, and a `..` path component) stay ACCEPTED. First, five committed tests in `tests/test_iter26_behavior.py` hold `/etc/**` and `""` schema-VALID through the CLI -- `test_walked_branch_discriminates_between_a_reaching_and_a_dead_glob`, `test_b1_absolute_glob_on_a_non_git_target_produces_a_document_not_a_traceback`, `test_b2_absolute_glob_gives_one_verdict_on_both_branches`, `test_b3_empty_glob_gives_one_verdict_on_both_branches` and `test_b3_empty_glob_does_not_fabricate_a_verdict_in_the_document` all five assert `radar scan` exits 0 -- three of them through the `_verdict` helper, which also parses the `--json` object and requires exactly one finding -- while three additionally assert stderr is EMPTY and one asserts the document opens `# Gap scan:`, so a load-time refusal turns the suite RED. Those five could be migrated onto `MALFORMED_GLOB`, so the rule is expensive rather than impossible, but the migration blunts the A/B they exist to prove: `test_fixture_teeth_absolute_and_empty_globs_still_break_pathlib` asserts those two patterns are the ones genuinely hostile to `Path.glob` on this interpreter, while `test_fixture_teeth_note_malformed_glob_is_not_a_pathlib_crash` records that `MALFORMED_GLOB` is not a crash here at all. Second, a `..` glob escapes nothing, so refusing it would be a QUALITY rule wearing containment's clothes: on a fixture whose parent held a marker file, `iter_files` with `../*.py`, `../sib/*.py`, `../x`, `a/../b`, `**/../y` and `../../**` each enumerated 0 files against 1 for the control `pkg/*.py`, putting `..` in the same match-nothing class iteration 26 chose for blank and absolute. A future scout should NOT re-pick this row to refuse the shape limbs; what stays genuinely open is only whether the register wants a separate QUALITY door, which is the same question row 62 asks for `pattern` shapes and is a different decision from this one |
| 62 | The quantified-wildcard rule lives only in the suite, so the ingest door can still admit one | open | Split from row 37 in iteration 27 DELIBERATELY, not deferred for size. That iteration's census asserts no pattern in `gaps/*.json` opens with a quantified wildcard, which brakes the class at the point a record is committed -- but the register is DATA fed by `tools/promote.py` from candidate JSON, and neither that gate nor `models.py` inspects a pattern's SHAPE, so a candidate carrying one is accepted and only the suite complains afterwards. Making it a schema rule or a gate refusal changes what two separate published surfaces accept (`docs/CONSUMER_CONTRACT.md` for the schema, `research/CANDIDATE_CONTRACT.md` for the door), and it needs the same decision row 61 needs: which pattern shapes are illegal, stated once, rather than a heuristic per door |
| 63 | `evaluate` copies a prefix slice of every file to count newlines for a hit's line number | shipped | Raised inside row 37's note in iteration 16 as a fold-in and NOT folded in by iteration 27, which shipped row 37 as a register-data-only change. `checks.py` computes a line number by slicing everything before the match and counting newlines in the copy; counting over a bounded range instead is provably byte-identical and copies nothing. Measured in iter 16 at 12.4 MB of string copies per scan and 0.2% of wall -- too small to be its own iteration, which is exactly why it needs a row: it should ride along with whichever of rows 35, 47 or 51 lands next, since all three already edit this function. SHIPPED in iteration 94 as the rider row 47 bite 1 carried, after being dropped by iterations 27 and 93: `text.count("\n", 0, m.start()) + 1` in place of `text[:m.start()].count("\n") + 1`, byte-identical line numbers and 10,555,378 bytes of string copies per scan deleted, re-measured at 120 records |
| 64 | `PRODUCT.md` has outgrown the index/archive split its own tooling assumes, and no `*_ARCHIVE.md` file exists | open | Measured in iteration 31 from the foundry's `doctor`: the file is 61,675 chars against a 54,000-char wall whose constant is commented "the wall the LIVE quality suite enforces", and it was ALREADY over at 59,515 chars before that iteration's edit, so the breach is structural rather than caused by one row. VERIFIED that the wall is not enforced in THIS suite -- 0 of 42 test files mention it, with a positive control proving the walk saw all 42 -- so it is the sibling product's wall and this row is an advisory, not a ship blocker. The remedy named there is an index/archive split: keep one terse row per item plus the `- iter NN` ledger here and move spent measurement prose to an archive file; raising the budget is explicitly NOT the remedy. Sequence it carefully, because rows are stable identifiers that committed docs and prior specs cite by number, so prose must move without any number moving |
| 65 | Four literal iteration-number pins make a ship commit red its own clean clone | shipped | Simulated in iteration 61 before touching the file: the product's own judges both return `[]` over `PRODUCT.md` plus a `- iter 61` row, while FOUR test assertions go RED -- a row count and two contiguity claims over ledger text (`test_iter31_behavior.py:158`, `:162`, `test_iter12_behavior.py:352`), plus `test_iter31_behavior.py:244`, the landmine, which compares REAL `git log` ships against `range(1, 32)` and so cannot fail until the ship commit exists. Iteration 60 relaxed two and was killed by the third; the fourth sits in a second file that this iteration's scout swept and cleared. Replaced by a floor, a historical-prefix check and the self-updating `unrecorded_ships`. Iteration 61's ship was REVERTED at the clean-clone pre-ship gate by a premise assertion inside its own new test file that was true only before that commit existed, and iteration 62 re-landed the payload with that assertion deleted and the durability sweep's domain widened to discover the current iteration's own module instead of exempting it |
| 66 | A truncated scan domain can produce an ABSENT verdict, the fail-open `checks.py` forbids in its own header | shipped | Raised as iteration 63's scout Candidate A1 with a DELETION remedy and re-scoped after measurement: the `[:MAX_SCAN_FILES]` slice at `checks.py:544` is the iterable of the loop performing the `_read` and the `regex.finditer`, so it bounds the dominant cost (independently measured at 80-95% of scan wall clock) and misses only the walk -- the cap STAYS. What is fixed is the silence: `RuleHit` gains `truncated_files`, every combinator propagates it by `max` over the sub-rules it evaluated, and `run_check` returns `UNKNOWN` instead of `ABSENT` when `present_when` did not match over a cut domain. Positive evidence is untouched and `MANUAL` is deliberately left alone, because `MANUAL` already denies safety while `ABSENT` is the only verdict that asserts it |
| 67 | `iter_files` re-derives the same file list many times per scan, while the DECODE beside it is memoised | shipped | Measured in iteration 63 against this repo as target: 34 `iter_files` calls over 8 distinct `(globs, exclude_tests)` keys, so 26 calls (76%) are redundant, costing 73.9 ms of a 453.8 ms scan (16.3%). Iteration 15 memoised the decode per scan for exactly this reason and left the enumeration per-rule, so this is the other half of that iteration. The lifetime must be the SCAN, not the process, for the coherence reason `read_cache_scope` already documents. Iteration 63's scout reported 35 calls over 11 keys against my 34 over 8; the discrepancy is UNEXPLAINED, so whoever takes this row re-measures rather than trusting either figure. Iteration 76 re-measured and the discrepancy is EXPLAINED rather than restated: 38 calls resolve to 11 distinct `(target, globs, exclude_tests)` triples over only 8 distinct GLOB SETS, because 3 glob sets are requested under BOTH `exclude_tests` values -- one count keyed the triple, the other keyed the globs. That decides the design instead of decorating it: all 3 of those pairs answer DIFFERENTLY (73 files vs 15, 90 vs 32, 73 vs 15), so a globs-only key would hand a 15-file domain to a rule that asked for 73, which is the fail-open class `checks.py` forbids in its own header. One key is requested 18 times, and 27 of 38 calls (71%) are redundant |
| 68 | Nothing bounds the WALK, the sort or the tracked-set intersection; only the reads and regex passes are capped | open | The half of iteration 63's finding it deliberately did not fix. `MAX_SCAN_FILES` is applied to what `iter_files` RETURNS, so a pathological target pays the full enumeration before any cap applies. Dormant at this scale -- the largest domain measured is 61 files against a cap of 4000 -- so it is a monorepo-scale concern, not a live one. The honest shape is a bound that REPORTS what it cut, matching the rule iteration 63 established that a cut domain may not produce a safety verdict; a second SILENT slice would reintroduce exactly the defect 63 closed |
| 69 | `UNKNOWN` acquired a SECOND meaning in iteration 63 and no published document says so | shipped | Measured by scout A in iter 65: `README.md:48` and `checks.py:52` both gloss UNKNOWN as "the check could not be run", but iter 63 added the OPPOSITE cause -- the check DID run, and its `present_when` non-match covered a domain `MAX_SCAN_FILES` had cut, so it may not assert safety. Control-paired counts: `truncat` 0 in README and 1 in the contract (unrelated SIGPIPE sense at :37); `MAX_SCAN` and `cut domain` 0 across all three docs. That is a FOURTH invariant of the family `README.md:52-56` publishes, unpublished, and it inverts on the SAFETY axis: a consumer reads UNKNOWN as a tool failure to retry when it is the tool refusing to certify safety. A brake can derive the requirement from the real `CheckOutcome` instead of pinning prose (the iter-64 precedent), because `tests/test_iter63_behavior.py` already monkeypatches `checks.MAX_SCAN_FILES` down over a `tmp_path` fixture. NOT taken in iter 65 only because the repetition brake forbade a fourth consecutive docs-shaped increment, not because it is low value Shipped in iteration 70. Re-measured with repr() per line before speccing, which found FOUR non-test surfaces rather than the two this row named: the rendered legend cell scan.py:263 and the rendered section heading scan.py:326 are bytes the tool emits on every scan, plus README.md:49 and the enum comment checks.py:52. One canonical checks.UNKNOWN_MEANING now says "no verdict: the check could not run, or its search was incomplete"; the legend reads it, the README cell is asserted EQUAL to it, the heading became "## No verdict (UNKNOWN)" derived from the enum value, and test_iter14's hand-copied UNKNOWN meaning became derived. docs/CONSUMER_CONTRACT.md glosses UNKNOWN nowhere, so it needed no edit. |
| 70 | `PRODUCT.md` is past the foundry's roadmap index hard budget, and this product never did the index/archive split | open | Measured in iter 65 with the foundry's OWN judges over the live file: `roadmap_index_budget` reports 71,015 chars against a 54,000 hard budget (headroom -17,015) and `roadmap_size_verdict` reports over_budget against 60,000. PRE-EXISTING and not caused by iter 65 -- HEAD was already -14,155. Neither judge is enforced by THIS product's suite: all 156 roadmap-reading tests pass on the edited file and iters 62-64 shipped over budget, so this is a CONTEXT cost rather than a red build -- every stage that reads the roadmap pays it, which is why it belongs on the roadmap instead of nowhere. The platform product fixed exactly this in its own iter 122: a terse LIVE index plus a tracked `*_ARCHIVE.md`, the move byte-for-byte, guarded by two pure brakes. Must NOT be bundled into a feature iteration -- the bytes are almost all in the Done ledger's long per-iteration bullets, so the split is a mechanical move plus its brakes. PRESCRIPTION REFUTED by iteration 94's scout B, measured on the live file and dated because a copied figure decays: the file is 147,515 chars (so both figures above are stale by 2.39x and 2.08x respectively, and headroom against the unchanged 54,000 hard wall is about -93,500), the Done ledger this row tells a taker to archive is 42,653 chars = 28.9%, and the table's Notes cells are 90,314 chars over 90 rows = 61.2%. So archiving the ledger ALONE leaves ~104,900 chars, still ~50,900 over the wall, and this row as written cannot fix its own problem. The prose that has to move is the per-row Notes, which is a materially different move because rows are stable identifiers cited by number in committed docs and prior specs, so an archive must be keyed by row id and no number may travel. `git ls-files` still reports 0 files matching `ARCHIVE`, so the split has never been done |
| 71 | `radar prd` emits its story list under a wrapper key the one declared consumer cannot parse | shipped | Iteration 68. Measured three times (iters 31, 61, 68), this time against the reader itself on live bytes: 3,693 B whose top-level keys end in the old wrapper, for which the consumer's `prd_status` returns `valid=False, total=0` and its dispatcher renders `prd.json present but unparseable`; renaming ONLY the wrapper to `stories` returns `valid=True, total=3, pending=('US-001','US-002','US-003')`, because every story object already carries `id` and `passes: false`. The Next-up paragraph's reason for freezing the default bytes -- that the old key is the convention `README.md` advertises -- is WITHDRAWN: `README.md` names no key, it advertises the SHAPE (ordered stories, `passes` flags, verifiable acceptance criteria), and the literal occurred only at `prd.py:190`, in nine test assertions and in roadmap prose, so by this register's own standard the convention claim carried no locator. The opposite claim carries two, both inside the consumer's tree: its reader accepts `stories`, and `render_prd_doc` -- that ecosystem's own prd.json PRODUCER -- emits that same key. Its roadmap archive also records that it CONSIDERED and DECLINED teaching its reader our key, because that widens a frozen control-path input parser, so the provider is the side that moves. Dual-key emission refused: a prd.json is a file the loop WRITES BACK to as `passes` flips, so two copies of one story list can diverge in the consumer's own hand and the reader may count the stale one |
| 72 | `_iter_tracked` and `_iter_walked` each carry their own copy of the glob match loop | shipped | Iteration 69. One shared `_match_globs`: both enumeration branches keep only their enumeration step and delegate, so an AST census over the module finds exactly ONE module-level function calling `_glob_regex` where it found two. `root` is DERIVED inside the helper instead of accepted as a parameter, which makes a caller publishing evidence from outside the target root unreachable rather than merely undocumented, and the helper stats unconditionally with no branch-selecting flag. The 19-line rationale for matching with `_glob_regex` and not `Path.glob` (the 3.12/3.13 trailing-`/**` divergence, `NotImplementedError` on an absolute pattern, `ValueError` on `""`) MOVES verbatim out of `_iter_walked`'s docstring to sit with the matcher. TWO SPEC PREDICTIONS MEASURED FALSE AND WITHDRAWN IN THIS SHIP: the net line delta is 691 -> 692, **+1, not the -23 scout A predicted**, because ~50 duplicated lines came out against a documented helper plus the relocated rationale; and `radar scan .` is NOT byte-unstable -- measured old-vs-new through the same CLI with the old file restored from `git show HEAD:`, it is byte-identical at 5,310 B and sha1 `9dfac59e` both ways, because the register's only locators into that file are `checks.py:98`, `:100` and `:115`, all ABOVE the first changed line at 230. That identity is conditional on the edit staying below line 115, not a property of the change. The reviewer proved the containment stat load-bearing rather than decorative by deleting `if not path.is_file(): continue` from a scratch copy of the package and re-running the same fixture, which then published `['gone.py','kept.py']`. Every published document is byte-identical (`repolens` 5,575 B, `agent-foundry` 6,622 B, self-scan 5,310 B), rc=0, stderr empty; no flag, key, verb or exit code is added |
| 73 | `taxonomy.STATUSES` is enforced on every record and published on no surface, while the contract tells the consumer to select on it | shipped | Iteration 71. `radar taxonomy` gains a fourth section, `## Record statuses`, naming every member with a gloss, and a brake INTROSPECTS the module's vocabularies so an enforced-but-unpublished one reds the suite: a candidate is an attribute table, and excluded, iff it is a Mapping whose member set equals a non-Mapping candidate's -- which is how `SOURCE_WEIGHTS` and the new `STATUS_GLOSSES` are excluded by MEASUREMENT rather than by name, so a sixth constant is covered too. Measured before speccing: stdout was 1,894 bytes with three `##` sections and zero of the four status values, and `partially-addressed`, `addressed` and `retired` occurred 0 times in `README.md` and in `docs/CONSUMER_CONTRACT.md`, whose `:142` row tells the declared consumer the field is required and selected on. Distinct from row 39, which needs a PRODUCT DECISION about whether a `partially-addressed` record stays citable and is deliberately NOT decided here -- publishing the vocabulary is that decision's prerequisite, since its own proposed restatement names `addressed` and `retired`. The stable-surface cell DROPS its three hand-restated counts rather than gaining a fourth, applying the rule `README.md:104` already publishes |
| 74 | `tools/verify_quotes.py` is the only tool with no test at all, and its verdict logic is unreachable by an offline suite because `verify` calls `fetch` directly | shipped | Iteration 72. The quote gate is the one door that can tell a fabricated quote on a real, resolving URL from a real one, and it had zero coverage (measured across 55 test files: promote 8, roadmap_integrity 5, check_locators 1, verify_quotes 0) because exactly one line of it needs the network and that line was unseamed, quarantining 100% of the VERBATIM/PARTIAL/NOT ON PAGE/UNREACHABLE decision. `verify` gains a keyword `fetch` seam defaulting to the module function, and the page normaliser is published as `page_text`, so every verdict path is now provable from literal HTML with no socket. The same hole was hiding a live fail-CLOSED bug, reproduced offline: the normaliser replaced EVERY tag with a space, so a character-perfect quote spanning an inline `<code>` span read `outputschema :` on the page and `outputschema:` in the quote and was reported NOT ON PAGE, with the PARTIAL fallback unable to save a quote shorter than PARTIAL_WINDOW; prose from the same page verified VERBATIM, which is the control proving the checker and not the page was wrong. A closed `INLINE_ZERO_WIDTH_TAGS` set (code, kbd, samp, var, tt) is removed without a space while every other tag still becomes one, and the fix is pinned two-sided: a block boundary must still separate `ends here` from `Starts there`, because collapsing tags indiscriminately would buy a false PASS in a gate whose only job is honesty. This is the SECOND instance of the one-sided-normalisation family the tool own docstring records, the first having been curly quotes normalised on one side only. |
| 75 | Every published statement of the `priority` rule is a PRODUCT, while `scoring` computes a weighted sum (3/2/1) | shipped | Iteration 74. Three surfaces state the rule and all three are false -- `README.md:11`, `docs/CONSUMER_CONTRACT.md:226` and `scoring.py`'s own module docstring -- while `diff.py:20` already carried the correct `(3/2/1)` form, so the repo contradicted itself and the wrong side was the published one. Cost, measured over the live 16 records: a consumer implementing the published product formula orders the register differently from `radar list` at 12 of 16 positions, and the contract obliges a file-reading consumer to reproduce the derived scores "the same way" -- whose only published referent is that false sentence. It survived review because its only two appearances in `tests/` are inline domain-control literals for the corroboration-claim finder, asserted to state no CONFIDENCE rule, which is true of them: a sentence used as a negative control is shielded from review of everything else it claims. Corrected on all three surfaces, plus a brake that DERIVES the required weight spellings from `W_SEVERITY`/`W_FREQUENCY`/`W_TRACTABILITY` at test time, so a weight change reds the documents instead of decaying quietly. |
| 76 | `radar diff --json`, a machine surface for the register comparison | shipped | `cli.py`, `diff.py`. `docs/CONSUMER_CONTRACT.md:371` prescribes a gate half that reads "non-regression (the diff did not reintroduce a known gap)", but `diff` emitted markdown only, so that half had no machine surface while `scan --json` and `list --json` both did. Emits both domain sizes, added/removed ids with titles, and per changed record the nine `COMPARED_FIELDS` with old/new; `priority` and `confidence` stay DISTINCT fields, never a blended score |
| 77 | `tools/check_locators.py` reports health after checking ZERO locators, and has no seam an offline suite can reach | shipped | Reproduced offline at rc=0 over a one-record register whose only locator is a bare DOI: `1 distinct locator(s), 0 broken`. `check_locators.py:58-60` `continue`s on a non-http locator and never counts it, so a register in which NOTHING is checkable prints a clean summary and exits 0 -- the third instance of the vacuous success rows 22 and 48 already ruled against, and the worst of the three, because this tool is the only thing standing behind the register's 167 distinct locators and `VISION.md` requires every record to carry a resolvable one. Four limbs, all of them shipped in iteration 79: the summary states distinct, CHECKED, SKIPPED and BROKEN with `checked + skipped == distinct`; an all-skip register exits 2 with one `Error: ` line naming 0 checked of N; a MIXED register still exits 1 and still prints its `BROKEN` detail line, which is the negative control that makes the refusal attributable rather than broad; and `main` gains a checker seam resolved AT CALL TIME (the `fetch_fn or fetch` form whose rationale is already written at `tools/verify_quotes.py:220-223`), which is what gives the tool its first test file -- under `tests/` the name occurred in exactly one place, a docstring in `test_iter24_behavior.py`, and its other four occurrences are prose in `README.md`, `src/agent_gap_radar/cli.py`, `tools/promote.py` and its own docstring. Locator SHAPE is deliberately untouched: a non-http locator stays schema-legal and stays a SKIP, because hardening it is row 57's open question and would contradict row 44's deliberate stop at non-blank |
| 78 | A register content pattern may anchor with a `re.MULTILINE` `^` and then scan to end of input inside a lookahead, which iteration 27's leading-wildcard brake cannot see | shipped | Iteration 81. Same class as row 37 / row 62 and the same remedy as iteration 27 (register data only), one shape further in: `test_iter27_behavior.py` refuses a pattern that OPENS with a quantified wildcard, and GAP-109 opened with `(?s)^(?=` instead, putting its two `[\s\S]*` scans INSIDE lookaheads where that brake has no reach. `checks.py` compiles every content pattern with `re.MULTILINE` unconditionally, so `^` matched at every line start and each one rescanned to end of file: measured 2,798.7 ms of one scan against 97.7 ms anchored with `\A`, 28.7x, identical `matched` and identical `locations`. Row 62's objection (the shape rule lives in the suite, not at the ingest door) applies to this brake exactly as it applies to iteration 27's, and row 62 stays open by the same deliberate split |
| 79 | The roadmap's `Next up` paragraph, the only statement of ship order, names rows the table reports `shipped` as forward candidates | shipped | Iteration 82. Measured at 1,921 chars on one line: a grouped sweep for `rows? N` with `and` / `,` / `/` continuations finds SEVEN row ids named and THREE of them `shipped` -- row 33 named as the #1 strongest remaining candidate while its own status cell reads `shipped` (iteration 73, `0a611c5`), plus 55 and 71 in a past-tense `were taken in iteration 68` clause. That is larger than the slate reported: scout B's sweep found five ids and missed 55 and 70 because it did not handle the `and` and `/` groupings, which is also why the brake must extract a GROUP of integers after `row`/`rows` rather than a single one. The paragraph carried four dead figures besides: it re-measured rows 64/70 at 75,567 chars against a live 105,161, and spent ~90 words on row 20's defect including `nine of ten seed records ... the live register holds 16, of which 15 do` against a live 120 of which 119 do, describing a sentence commit `e352240` had already deleted. Nothing read it -- `Next up` occurs in exactly one file and zero tests -- while `roadmap_integrity` already parses this table's rows and statuses and is the one brake a ship commit cannot dodge. Remedy: rewrite the paragraph to name only `open` rows and restate no counts, plus `ship_order_violations` derived from the Status column, which also refuses a MISSING pointer and one naming ZERO rows, because a rule that passes over nothing is the vacuous-success class rows 22, 48 and 77 already ruled against and `vacuity_violations` already states in its own docstring |
| 80 | `tools/promote.py`'s refusal set is published incompletely, and the one refusal that decides whether a research pass lands at all is published nowhere | open | Iteration 83 scout A, measured: the tool's own docstring and `research/CANDIDATE_CONTRACT.md` both enumerate five gates (Schema, Evidence, Fixtures, Novelty, Twins) while `README.md:67` names FOUR, omitting Twins -- and the code's docstring says gate 4 cannot substitute for it, because parallel agents restate one gap with differently-spelled detectors. A SIXTH path is in none of the three: the `--register-cap` ceiling, shipped and covered by two tests, whose reasoning exists only in a code comment, and `rg -n "capacity|register.cap" README.md docs/ research/` finds it nowhere. Remedy: one module-level tuple naming every refusal path, with both documents compared against THAT rather than against each other, in both directions -- the pattern already proven three times here (`EXIT_CODES` vs the contract's exit-code table, the `scan --json` key list, the pydantic-import table). Deferred in iteration 83 under the diversity guard, since iterations 79 and 82 were both document-integrity brakes over this repo's steering prose. The brake would prove COMPLETENESS and never correctness: a name-set check passes over a sentence that is wrong about what a gate does, which is what iteration 64 had to fix in the README's confidence sentence, so the test docstring must say what it cannot see |
| 81 | `radar list --json` does not publish WHICH layer filter produced a payload, so a filtered object and a whole register are indistinguishable | open | Split from #16 on RISK, not size. Iteration 31 published the confidence floor `scan --json` applied for exactly this reason, and rows 30 and 34 fix the same silent-wrong-answer class on the same surface. Deferred because a top-level key re-baselines two committed key-set pins -- `test_b7_top_level_shape_and_key_order` (`tests/test_iter01_behavior.py`) and `test_b7_list_json_stays_a_stable_object_with_no_new_key` (`tests/test_iter07_behavior.py`) -- and re-baselining a pin whose stated job is refusing new keys is its own decision, not a rider on a filter. Decide the KEY once, because any future filter on this verb asks the same question |
| 82 | `verify()` binds its `fetch` seam as an IMPORT-TIME default, so substituting the module's fetch never reaches the only path `main()` runs | shipped | Iteration 85, the residue of row 74. `fetch_all` resolves late (`fn = fetch_fn or fetch`) and its own comment says the alternative would "silently ignore any later substitution of the module-level fetch"; `verify` is the alternative -- `def verify(records, fetch: _FetchFn = fetch)` evaluates the default once at definition. Measured offline through the real module, identity only, no request issued: after `setattr(module, "fetch", fake)`, `module.fetch is fake` is True while `verify.__defaults__[0] is fake` is False and `is` the real fetch. `main()` calls `verify(records)` with no seam, so that IS the production path, and three test modules patch exactly the attribute the default cannot see (`test_verify_partition.py`, `test_verify_partition_malformed.py`, `test_verify_bounded_selection.py`) -- safe today only because they call `partition`, which routes through the late-bound sibling. The failure mode is the one class no later run can detect: a test that PASSES while opening a real socket, against the first line of the quality bar. Fixed by resolving the module-level function at CALL time through a one-line indirection, since the parameter shadows the name; the `fetch=` KEYWORD is deliberately KEPT rather than renamed to the sibling's `fetch_fn`, because 12 committed call sites in `tests/test_iter72_behavior.py` pass it and iteration 72 published it. `test_b8_the_seam_default_is_the_modules_own_fetch` asserted the defect verbatim (`default is verify_quotes.fetch`) and is INVERTED in the same commit, not deleted, so the fence still fails if the import-time binding returns |
| 83 | The JSON document tail is written out four times and the committed one-tail census cannot see any of them | shipped | Iteration 87. The markdown tail has exactly ONE implementation (`render.document`) and a two-sided census in `tests/test_iter14_behavior.py` proves it; its sibling -- `json.dumps(<expr>, indent=2, sort_keys=False) + "\n"` -- occurs 4 times, at `cli.py:148`, `prd.py:195`, `scan.py:262` and `diff.py:279`, and the census is STRUCTURALLY blind to every one of them because `TAIL_IDIOM` requires the `lines.pop()` prelude no `json.dumps` line can carry. So the repo publishes a one-tail guarantee, proves it for markdown, and says nothing about JSON -- on the four surfaces `docs/CONSUMER_CONTRACT.md` points a CI gate at. It also RATCHETS one copy per `--json` verb (3 from iteration 18 through 76, 4 since iteration 77 added `radar diff --json`; open row 81 is exactly such a verb). Re-measured before speccing and scout A's slate CORRECTED: the expression is byte-identical at only 3 of the 4 sites -- `prd.py` passes `prd_for(gap, project)` where the others pass `payload` -- which is why the new census rule is keyed on the AST SHAPE rather than on a literal, since a literal census would have reported 3 and read as healthy |
| 84 | The stable-surface table demands four argument tokens the CLI makes OPTIONAL, and the derivation iteration 30 shipped cannot see bracketing at all | shipped | Iteration 88. Iteration 30 made the table an assertion DERIVED from `build_parser()`, but that derivation reads flag NAMES, option ARITY and POSITIONAL COUNTS only -- `tests/test_iter10_behavior.py:173` does `token.strip("[]")` before comparing, so the table's own `<x>` required / `[x]` optional notation is invisible to it. Requiredness was checked by exactly ONE narrower assertion, `tests/test_iter11_behavior.py:709`, scoped to the `diff` row and to one direction (a REQUIRED positional documented as optional), and every live drift instance sat on the unchecked side of both scopes. Measured off the parser (`action.required` for options, `nargs not in ("?", "*")` for positionals): `validate <repo>` and `report <repo>` are `nargs="?" default="."`, and `prd <repo> --gap <ID>` is wrong TWICE -- four token-level disagreements in three rows. The `--gap` cell is the one that costs something: `radar prd` with no `--gap` selects the top-ranked gap and `README.md`'s quickstart publishes exactly that, so the two documents contradicted each other about one flag and the MACHINE-facing one was wrong. This is the MIRROR of iteration 30's defect -- those rows named LESS than the CLI accepts, these demanded MORE than it requires -- so the fix is one-directional OMISSION repaired in the opposite direction, and a one-sided check would pass the table unchanged either way. `README.md` is deliberately NOT edited: it was the accurate document. Zero `src/` lines and zero emitted bytes move |
| 85 | The harness that proves this suite can FAIL is an orphan: no release path can fail on it, and one drifted anchor disarms all 20 planted defects | shipped | Iteration 90. Measured in that run: the token `verify_mutations` occurs in exactly ONE tracked file -- its own docstring -- so `pyproject.toml`'s `addopts` never reaches it and no stage runs it, which is GAP-120 (an eval suite exists but no release path can fail on it) running inside the repo that publishes GAP-120. Row 74 shipped for this exact shape when `tools/verify_quotes.py` was the only tool with no test; this tool then held that title. Three pre-flight predicates become pure functions over injectable seams (`anchor_defects`, `residue_defects`, `oracle_defects`) and are asserted in-suite, so a refactor that retires one of the 20 hand-written anchors -- 12 in `tools/promote.py`, 7 in `tools/verify_quotes.py`, 1 in `src/agent_gap_radar/cli.py`, all three files this loop refactors ON PURPOSE -- reds `uv run pytest` instead of taking the harness offline behind a message that reads as a tooling error. The residue half is a brake and starts green (0 of 20 mutated forms on disk): restoration is a `finally` block, which SIGKILL does not run, and the foundry's `stage-budget` line put the worst stage at a 600.0s median with 0.0s headroom, so a cap-killed plant leaving `if mine is None or True:` -- the twins gate disabled, the defect that once refused 849 of 859 candidates -- in a tracked file is the median case rather than an edge case. The brake asserts the harness is ARMED, never that it still CATCHES; the 20 subprocess runs stay out of the suite deliberately |
| 86 | The behavioural twin test can only ever compare a NEW candidate against the register, so known-vs-known overlap has never been computed | open | Raised as iteration 90's scout A candidate A1 and deferred on measured stage budget, not on merit -- `doctor` put the worst stage at a 600.0s median with 0.0s headroom and this is a new tool plus an O(n^2) sweep. Measured there over the live 120 records through the ingest door's own `_Twins._stands_in_for`: 114 records are comparable (1 has no check, 5 have a check but no fixtures), 76 of 12,882 ordered pairs are one-way subsumptions (0.59%), 43 of 114 (38%) have their bad/good evidence reproduced by at least one OTHER record's detector, and MUTUAL twins are ZERO -- so the ingest gate held and the whole curation debt is one-way. Broadest detectors: GAP-043 stands in for 6 others, GAP-020 for 5, GAP-025 / GAP-030 / GAP-037 for 4 each. 17.2s wall, 25,764 `run_check` calls, identical edges across two runs. Two decisions ride with it: it ANSWERS row 11's question over a domain row 11 never distinguished (prevention at the door is shipped; detection over records already held is not), and row 11's prescribed mechanism -- normalised title tokens -- is REFUTED by the measurement `tools/promote.py`'s own docstring records over 276 hand-labelled pairs, so build it behaviourally or it ships a known-bad gate. Shape: a `tools/` script beside `promote.py`, because `docs/CONSUMER_CONTRACT.md` publishes the rule that fixture-materialising work stays out of `radar` |
| 87 | 19 of the 20 planted defects target `tools/`, so 10 of 11 `src/` modules carry no mutation and only 5 of 76 test files are ever named as an oracle | open | Measured in iteration 90 by scout B and split from row 85 on RISK, not size: row 85 asserts the harness is loadable and armed, which is a property of its DATA, while adding `src/` mutations changes its runtime and its risk profile. The uncovered set includes `scoring.py`, which derives the confidence `VISION.md` names as the one rule it protects, so the suite holds no proof today that it would notice that derivation breaking |
| 88 | The PUBLIC REPO clause of the quality bar is enforced by nothing committed, over a tracked set whose majority is register data written by an outside research pipeline | shipped | Iteration 91. Measured before speccing: 224 tracked files, 120 of them gap records built by the research pipeline from arbitrary primary sources, and NONE of the five published refusal gates (Schema, Evidence, Fixtures, Novelty, Twins) is a public-safety gate -- the tokens for machine paths, addresses and leak vocabulary occur 0 times in `tools/promote.py` and 0 times in `src/agent_gap_radar/models.py`. Enforcement was per-iteration and ad-hoc: exactly 2 of 78 test files carry banned-token literals and both are scoped to the one or two documents their own iteration edited, so nothing covered the other 222 files. The DX half is the recurring cost -- iteration 90's final gate hand-rolled its own leak grep, the built-in `grep` is ripgrep and parsed `-E` as `--encoding`, and the check's or-else fallback printed the reassuring word CLEAN over zero opened files. Shipped as `tools/check_public_safety.py` over the `git ls-files` domain rows 18, 30 and 69 already establish, fail-closed on an empty domain and on an unreadable file, with the verdict reachable offline through call-time seams. The rule set is DATA, four rules, each measured two-sided against the live tree AND against a planted sample before it was written. NO exemption list and no self-exclusion: every pattern requires at least one name character after its marker and the file never places one there -- each marker is held exactly once as a bare string literal terminated by that literal's closing quote, which is not a member of the name-character class, and `_path_rule` composes the pattern from that variable instead of inlining the marker beside the class -- so the scanner scans itself and stays silent. The invariant a future editor must preserve is therefore one rule, stated in the docstring: never inline a marker literal into a pattern or a sample, because that is the edit which would put a name character after a marker and red the gate on its own source. This is the trap iteration 06 hit live when its self-scanning version failed on its own assertion line. Two candidate rules were REJECTED on measurement rather than on taste, recorded here so nobody re-adds them: a windows-drive-path rule fires 467 times across 108 tracked files on correct data, because gap records are JSON and an escaped newline inside a verbatim code quote reads as drive-letter plus backslash plus name character, and an ascii-purity rule would red the 7 tracked files that legitimately carry non-ascii verbatim quotes, so the quote invariant outranks it. Real names and employer tokens are deliberately NOT checked: a name list committed to a public repo IS the leak it claims to prevent |
| 89 | `tools/verify_mutations.py` counts ANY non-zero pytest exit as CAUGHT and runs no green control, so an oracle that cannot RUN certifies a mutation | open | Measured by iteration 91's scout A through this repo's own `.venv/bin/pytest` with the harness's own flags: exit 1 a real assertion failure (CAUGHT, correct), exit 5 an empty or non-collecting oracle and exit 2 a collection or import error (both CAUGHT, unearned), exit 0 green (MISSED, correct). `oracle_defects` checks only that the oracle file EXISTS, so an emptied oracle passes the armed pre-flight and then returns 5 -- and 10 of the 20 mutations name one file, `tests/test_promote_twins.py`, so a single emptied file silently converts half the harness into unearned passes while every in-suite brake stays green. This is the fail-open class rows 22, 48 and 77 already ruled against, arriving on the one artifact that certifies the suite can fail at all. LATENT today rather than live: every one of the 20 mutated forms compiles, applied in memory with nothing written. Remedy: require the real failure code instead of any non-zero one, add ONE green control per DISTINCT oracle file (+47.4s measured, against +194s for one per mutation), and route the subprocess through a call-time seam so the verdict logic is reachable by the offline suite -- the identical move rows 74 and 82 made for `verify_quotes.fetch`. Deferred on stage budget, NOT on merit: a stricter verdict is a behaviour change to a release-path brake, so the slice must demonstrate all 20 still report CAUGHT, which is one real ~194s run inside a 600s stage whose engineer median already measures 600.0s with 0.0s headroom. Sequence this BEFORE row 87, because adding entries to a harness whose CAUGHT verdict can be unearned multiplies a signal that is not yet trustworthy |
| 90 | The machine payloads a release gate consumes cannot answer the published gate rule's `is open` clause | shipped | Iteration 92 settled the PIN this row said it owed, then spent one key on each payload: `scan --json` findings carry `status` as a 13th key and `prd`'s `sourceGap` as an 8th, both APPENDED so every pre-existing key keeps its index and even a positional reader is unbroken. The pin's intent is stated once -- no key RENAMED and no key REMOVED, growth permitted -- which is the only reading compatible with this contract's own "New fields MAY be added at any time", and `tests/test_iter23_behavior.py`'s literal already had that append shape. PUBLISHING IS NOT SELECTING: two registers differing only in one record's status emit identical findings, counts and prd selection, so never-drop is untouched and the consumer's gate keeps the decision. Re-derived rather than inherited: 120 records, 117 `open`, 3 `partially-addressed`. The scan half is LATENT on this target (all three verdict ABSENT or NOT_APPLICABLE) and the prd half is LIVE -- `radar prd . --gap GAP-016` exits 0 today and hands a loop a partially-addressed gap with no signal, and the PRD is the document that enters that loop's prompt. Original measurement, kept: Measured by iteration 91's scout B: `docs/CONSUMER_CONTRACT.md` tells a CI gate to check that a cited gap exists, is open and is above the floor, but `scan --json`'s findings carry 12 keys and `prd`'s `sourceGap` carries 7, and `status` is among neither -- `scan.py` and `prd.py` never read it, the only three reads in `src/` being `diff.py`, `render.py` and `cli.py`'s `list --json`. So two of the three clauses are answerable from the payload and the middle one is not. Live register: 120 records, 3 of them `partially-addressed`, one being GAP-016 at rank 3 on priority 9.0 -- the case where the clause would bite. LATENT on both real scan targets today (GAP-016 ABSENT, GAP-012 and GAP-101 NOT_APPLICABLE). Small: one key per payload. The decision this row owes is not the key but the PIN -- adding it re-baselines two committed key-set assertions, one of them named for staying a stable object with no new key, so either that pin's intent is narrowed deliberately and in the same commit, or the clause is dropped from the contract instead |
| 91 | A roadmap row whose defect is provably gone can stay `open`, and no committed brake can see it | open | Iteration 94, measured by scout B and acted on in the same commit for the two rows it found: rows 20 and 49 read `open` while each stated defect was measurably ABSENT from the tree, one fixed outside the loop (`e352240`) and one fixed by iteration 64 (`c591b7e`) and enforced since by a committed brake. Both were flipped in iteration 94 as a PM status correction, so the two known instances are closed and what remains open is the MECHANISM. Every finding in `tools/roadmap_integrity.py:main()` -- vacuity, two-value status, ledger sequence, legend, ship order, `unrecorded_ships` -- reads the document against itself or against git subjects, so a ledgered fix that never flipped its own Status cell is invisible to the brake, to every pre-commit stage and to preship, while `ship_order_violations` keeps it an eligible forward candidate. Remedy: a doc-brake declares the row it discharges as an explicit module constant (`ROADMAP_ROW = N`) and one new check requires every declared row to read `shipped`, with `tests/test_readme_confidence_rule.py` -> 49 and `tests/test_contract_verb_headings.py` -> 36 as the two day-one declarations. REFUTED on the way there and recorded so it is not retried: the cheap version that SCRAPES `row N` out of test prose is unsound -- 12 of 81 test files already name a row and the mentions are mostly the opposite of a discharge, so it would red five correct `open` rows on day one, enumerated rather than counted: 11, 28, 51, 57 and 64. Narrow by construction, since it covers only rows that HAVE a brake |
| 92 | The suite wall is over the foundry's throughput threshold, and nothing has measured WHERE it goes | open | Iteration 94: `SPEED_STORY_NEEDED.md` reports a fresh-clone suite wall of 163.87s against a `SUITE_SLOW_SECONDS` threshold of 120.00s at sha `fbf86b2e5`. ADVISORY and non-blocking, but it is a real tax on every iteration rather than a number in a flag: the engineer stage median already measures 600.0s with 0.0s headroom against the hard cap, and the suite runs inside it. NOT a candidate on unmeasured grounds -- iteration 94's scout A declined to price it because one run costs ~164s of a 600s stage, and the product-side throughput rows measure `scan`, not the BUILD, so the two are related only through the suite's own scan calls. What was measured cheaply is its likely shape: 49 `subprocess.run/Popen` call sites across 20 test files, concentrated in `test_iter91_behavior.py` (10), `test_checks_fixtures.py` (5) and `test_iter69/26/25` (4 each), each spawn paying a fresh interpreter start plus a pydantic import, and `pytest` already runs `-q -n auto` so the remaining lever is spawn count rather than parallelism. FIRST STEP IS A MEASUREMENT, NOT A FIX: one `--durations=25` run to settle whether the 164s is spawns, collection, or a handful of slow tests. The flag auto-clears on the next genuine fast ship, so it may clear itself before this row is taken |
| 93 | The README names none of the other tracked Markdown documents, so the contract a CI gate is pointed at is unreachable from every user-facing surface | shipped | Iteration 95, documentation plus a derived brake, with ZERO files under `src/agent_gap_radar/` touched -- which is why no renderer, golden, exit code, JSON payload or verb-surface pin can move. Measured over the live tree before speccing: `git ls-files '*.md'` names 6 documents and `README.md` named 0 of the other 5, checked three ways per document (repo-relative path, bare basename, basename stem), and the substring `.md` occurred ZERO times in the committed file. The expensive part was WHERE the omission fell rather than the missing list: the README quickstart hands a reader `radar scan ... --gaps gaps --json` for a CI gate, and the object that invocation emits is specified only in `docs/CONSUMER_CONTRACT.md` -- 407 lines, named by 5 of the 11 `src/` modules, by 19 test files as the repo-relative path (22 counting the bare basename) and by 1 `tools/` script -- which appeared in neither the README nor `radar --help` (923 chars of stdout, zero `.md` substrings), so a reader was pointed at a payload whose contract no user-facing surface names. The same hole existed on the research side, where the README describes the gated inbox and names `tools/promote.py` while the rules that gate it live in `research/CANDIDATE_CONTRACT.md`. Ships as one `## Documents in this repo` section whose every row carries a provenance word from a CLOSED two-value vocabulary, `hand-maintained` or `generated by the build loop`, because this file and `DIRECTIONS.md` are harness-written and a contributor who hand-edits them loses the edit. The brake is a tests-local oracle of pure functions over `(readme_text, tracked_paths)`, proved TWO-SIDED against synthetic input because the committed file can only ever demonstrate the passing side, and BOTH directions are checked: the live drift was one-directional OMISSION, so a containment rule alone passes the pre-change README exactly as it stood, having named no paths at all, and proves nothing. Rows are matched by path TOKEN and never by substring, since `docs/CONSUMER_CONTRACT.md` contains `CONSUMER_CONTRACT.md` and a bare basename is not a path a reader can open from the repo root. THE DECISION THAT FOLLOWS, recorded so a later iteration does not "fix" it: the expectation is DERIVED from `git ls-files` through the seam `tools/check_public_safety.py` already settled, so when rows 64 and 70 eventually add another tracked Markdown document this brake goes RED until that document gets a README row -- that is the brake working as designed, and the repair is a README row, never a hard-coded list here, because a hand-written document list in a test is the same artifact as the missing list in the README one directory over and drifts the same way |
| 94 | `radar tags`: publish the cross-layer axis every record carries and no surface reads | open | Iteration 95 triage, DECLINED on measured size against a measured stage budget and never on merit -- the numbers live here so the next PM inherits them instead of re-deriving them, and every one below was re-derived from the live register in that triage rather than copied: 120 of 120 records carry `tags`, 542 mentions, 275 distinct values, 175 of them singletons, and 55 tags span more than one layer. That last figure is the capability -- this is the register's only cross-layer axis, and a builder asking what the register knows about one theme cannot get an answer today, because `report` is layer-ordered and `list --layer` shows exactly one layer. The vocabulary is also fragmenting where nothing can see it: the most-used tag is split across two spellings, `evals` at 16 records and `eval` at 4, so any count of it is wrong by 4 records until a curator can see the pair. The field is written by every record and read by none -- the only occurrence in `src/` outside its `models.py` declaration is a `diff.py` docstring naming it among the fields deliberately NOT compared, and the literal `"tags"` occurs 3 times across the suite, all synthetic fixture data -- which is the same shape as row 73, an enforced-but-unread field, and that row shipped. WHAT MAKES IT EXPENSIVE, and the correction to the slate that estimated it re-baselines none of the committed pins: a ninth verb reds two SET-EQUALITY pins on the verb surface, `tests/test_iter16_behavior.py` asserting `parser_choices()` equals a frozenset of the eight shipped verbs in a test named for that surface and `tests/test_iter87_behavior.py` asserting the argparse usage brace list verbatim, on top of a new subparser, a new renderer, a `## The stable surface` table row held by set equality through `tests/_surface_contract.py`, and a README quickstart line. CHEAPEST SPLIT, offered as the shape those pins imply and NOT as a measured estimate: bite 1 ships the census as a pure derivation with its own two-sided tests and no parser change -- per-tag record count, layers spanned, singleton count and the normalised collision groups -- reaching no verb and moving no rendered byte, which is the dormant-half-first move rows 51 and 93 already used; bite 2 then spends the verb-surface pins on the surface that renders it. TWO CONSTRAINTS THE ROW INHERITS: the one published sentence about this field calls them `free labels, NOT a closed vocabulary` and says `never switch on one`, so the capability must be a HUMAN curation census and never a selector -- which rules out a `--tag T` filter and any `--json` payload a gate could key on -- and at 275 distinct values the full census needs a stated bound with its elision ANNOUNCED rather than silently truncated, because 175 of those rows would each read 1. It must also not live inside `radar taxonomy`, whose module exists to publish CLOSED enumerations |
| 95 | The stderr-purity contract is stated in a docstring and in the quality bar, and NO mechanism holds it | shipped | Iteration 96, a suite brake with ZERO files under `src/agent_gap_radar/` touched. `checks.py:148-160` says of the glob character-class escape, verbatim, "this tool contracts that stderr carries `Error: ` lines only, so a warning there is itself a defect", and names the instance it prevents, `FutureWarning: Possible nested set`. Measured before the pick: the literal `filterwarnings` occurred ZERO times in the repo and `[tool.pytest.ini_options]` carried exactly `addopts` and `testpaths` (read with `tomllib`). The ~100 `err == ""` assertions are per-test, so a warning on a path whose test does not capture stderr was invisible -- the fail-open shape of a mitigation that exists only where someone thought to look. Cost was measured, not assumed: the full suite ran green in the planning stage under `uv run pytest -W error -p no:cacheprovider` (`3077 passed, 5 skipped in 124.28s`, exit 0), a configuration at least as strict as the ini form, so no warning backlog existed to clean first. Two narrowings were REJECTED with reasons rather than on taste, and a later iteration should not re-derive them: a module-scoped filter (`error:::agent_gap_radar.*`) would MISS the founding case, because that `FutureWarning` is raised inside stdlib `re` while compiling a pattern the product built, and a mechanism that misses its own founding case is decorative; and this brake makes NO claim about the shipped CLI at runtime, where a user's interpreter runs Python's default filters. The escape itself is unchanged and now carries a two-sided proof whose control is position-sensitive: `re.compile("[" + "[" + "]")` warns, while a body of `a[` warns not at all, so a control built on that body would pass at every filter setting. |
| 96 | The Done ledger cannot record iteration 100: the number rule is an exact length of two, and the suite's own derived-future rows already cross that ceiling | shipped | Iteration 98, a one-predicate fix to a committed brake. `ledger_sequence_violations` flagged `not-two-digit` on `len(raw) != 2`, so no ledger row above 99 can ever be legal -- and the ceiling is not three iterations away, it is HERE, because two committed tests derive iteration numbers ABOVE the live maximum. Measured by driving the real helpers on three texts rather than by reading them: at HEAD (max 96) `test_iter62::_grown` appends 97 and 98 and `test_iter31::test_b8_the_floor_admits_a_later_well_formed_row` then appends 99, all two-digit, suite green; with a `- iter 97` row (max 97) the b8 row becomes 100 and the suite reds with exactly `SequenceViolation(kind='not-two-digit', iteration='100')` -- which is the whole cause of iteration 97's revert, a green feature and a green tester module lost to its own roadmap row; with a `- iter 98` row (max 98) `_grown` ITSELF reaches 100 and b8 reaches 101, so iteration 98 reds one step EARLIER than 97 did and with two violations instead of one. The consequence is a shipping ceiling, not a cosmetic one: `unrecorded_ships` makes exactly one ledger row per shipped iteration a ship PRECONDITION, so from iteration 97 onward the precondition and the suite contradict each other and NOTHING can ship, whatever it builds. The fix reads the rule the docstring already states -- `2` and `02` parse to one integer and only one of them is a valid record, i.e. the rule is about canonical TEXT -- and spells it as `raw != f"{value:02d}"`. That predicate is pointwise IDENTICAL to the old one across 1..99 (`2` is still a violation because its canonical form is `02`; `02` is still clean), which is why the four existing kind-name assertions and the pinned vocabulary need no edit, and it accepts `100` while still rejecting the over-padded `0100`. The published kind `not-two-digit` is deliberately UNCHANGED: it is a vocabulary pinned by a committed brake at `tests/test_iter04_behavior.py:258` and quoted into user-facing findings through `SequenceViolation.message`, and it stays true under the reading the fix makes explicit -- the pad is a MINIMUM width of two, not an exact length, so `100` satisfies it and `0100` does not. Renaming it would widen a release-unblocking fix into a published-surface change across five test sites, and a rename the reviewer disagreed with would block the unblock. |
| 97 | `radar show` renders nothing from `Check.rationale`, the largest authored field in the register that no surface reads | shipped | Iteration 108. `rationale` is the sentence that says WHY a signature indicates the gap and what it can get wrong -- what a builder needs before trusting a verdict -- and the token occurs across all of `src/` and `tools/` exactly ONCE, its own declaration at `models.py:140`, with no `.rationale` attribute access anywhere in either tree. Measured on the register as it stood: 120 records, 119 carry a check and 119 of 119 carry a non-empty rationale totalling 76,853 chars, median 645, min 78 (GAP-002), max 1,206 (GAP-032) -- a larger body of authored text than everything `## Evidence` renders per record, and the same shape as row 73 (the status vocabulary enforced on every record and published on no surface) and row 13 (a coverage view that dropped the two facts it existed to publish). Iteration 29 gave the brief a `## Detection` block that publishes the check id, the derived detectability and the manual question, so the deep view states THAT a signature exists and never why it is believed. Two limbs, because the display limb alone would be dishonest: the block gains one final `- Rationale: <value verbatim>` bullet, untruncated and unwrapped with an explicit `none recorded` for an empty one and no bullet at all for the record that carries no check (GAP-010); and `models.Check` gains the guard that keeps the bullet renderable, refusing a rationale holding a newline or differing from its stripped form. The guard is a REPRODUCTION rather than a precaution: measured through the real loader, a rationale of `line one\nline two` validates today at rc 0 with `OK: 1 gap record(s) valid.`, and such a value would break iteration 29's own single-contiguous-block pin days later on somebody else's iteration. It starts green -- across the 119 live rationales, newline 0, `|` 0, leading `#` 0, odd backtick count 0, untrimmed 0, non-ASCII 0, backslash 0 -- and the field stays OPTIONAL at the door, because refusing an empty one is a different decision and the renderer publishes that absence instead of hiding it. No verb, flag, JSON key or exit code moves, and no machine payload gains the field |

Status values are exactly `open` or `shipped` -- there is no third value. A row is flipped to `shipped` in the same commit that lands it, together with that iteration's Done-ledger row; the iteration that shipped a row is recorded in the ledger, never in this column.

Numbers are stable identifiers, not an ordering -- rows are appended, never renumbered, because committed docs and prior specs cite them. Ship order is the line below.

**Next up:** this paragraph is the only statement of ship order, and it may name only rows the table above reports `open` -- `tools/roadmap_integrity.py` derives that from the Status column and enforces it, so the commit that flips a row named here to `shipped` must rewrite this line as well, exactly as it must add that iteration's Done-ledger row. No register or file figure is restated here, because a hand-copied count decays on every research pass and this paragraph had accumulated four dead ones; measure the file or run `radar report .`. A row id is named ONCE here: the tool's sweep reads this line de-duplicated while a committed independent reader keeps duplicates, so a second mention of the same row makes the two disagree, and a later clause points back in words instead. The strongest remaining candidates, in order: row 47, whose remaining bite is the boolean-only and `exclude_tests` propagation shape, held back from the earlier bite of the same row because that propagation has already produced a fail-open in this module; then row 89, whose CAUGHT verdict can be unearned on the one artifact that certifies this suite can fail at all, and which must precede any row that adds entries to that harness; then rows 64 and 70, the index/archive split this product has never done, one problem described by two rows, which must not ride along with a feature and whose archiving prescription is now recorded as REFUTED by measurement -- the bytes are in the per-row Notes rather than in the Done ledger, so the move is keyed by row id and no number may travel; then rows 61 and 62, which are not a build but one PRODUCT DECISION owed -- whether the register wants a separate QUALITY door for `pattern` and `globs` SHAPES, stated once, instead of a heuristic per door; and row 91, which narrows the class this iteration found the hard way, a row whose defect is provably gone staying `open` and remaining an eligible forward candidate because every check here reads the document against itself and none tests a row against the tree. Row 80 is deferred for the FOURTH time and the repetition is the point rather than a footnote: what blocks it is not size but an undecided direction, either the code falling through to the single summary emitter or the contract gaining an except-at-capacity clause that weakens a rule whose value is being unconditional -- decide that and it becomes a small increment. Row 92 is a MEASUREMENT owed rather than a build, and it may clear itself before anyone takes it. Row 94 is newly UNBLOCKED rather than newly attractive: it was declined on size when the slate first offered it, taken in the reverted iteration, and lost there to the ledger ceiling this iteration removes, so its patch and its tester module are preserved under `state/iter-97/reland/` and a taker re-lands rather than rebuilds.

## Non-goals for this roadmap

No web fetching inside the tool. No LLM calls. No dashboard. No database. The register is files in git, and that is the feature.

## Done ledger

One row per iteration, landed in that iteration's own ship commit. Deferring a record is how history gets permanently lost. An iteration whose ship commit never landed says so in its own row -- `unrecorded_ships` is one-directional by design, so the ledger is the only place a failed iteration can be explained.

- iter 01 `radar list` stops dropping below-floor records and gains `--json`; three false doc rows made true
- iter 02 `radar scan --prd` no longer builds against a below-floor finding; skips announced, exit 2 when none clears
- iter 03 `scan` walks the tracked set instead of globbing the tree, 12x faster; row recorded late, in iter 04
- iter 04 retires the ambiguous `iter NN` status; the suite now fails when a shipped iteration has no ledger row
- iter 05 below-floor rows name the cheapest citation class that lifts them, derived from `confidence()`
- iter 06 `radar validate` fails closed on a domain holding zero records; the contract's false `iff` made true
- iter 07 "Strongest source" comes from the ladder rung; alphabetical min() could print `model-output` as best evidence
- iter 08 `rank()` + `below_floor()` become one single-pass partition; never-drop is asserted, not coincidental
- iter 09 two live-register id censuses become `confidence()` oracles; the report's SECTION membership is now asserted
- iter 10 the contract's stable-surface table becomes a parser-derived assertion; 5 rows named less than the CLI accepts
- iter 11 `radar diff OLD NEW` built, reviewed APPROVE and tested green, but its ship commit was orphaned by the stage cap; re-landed unchanged by iter 12
- iter 12 re-lands iteration 11's orphaned `radar diff` commit unchanged; zero new behaviour, and the ledger now explains the gap in git history
- iter 13 `scan --json` publishes the floor it applied and flags every finding; two emitted keys were undocumented
- iter 14 `render_scan` reaches published `document`/`table` instead of copying them; a census brakes the one-newline tail
- iter 15 scan-scoped read cache: 4,428 decodes become 224 over 226 distinct files; row 29's "one cost" claim retracted at 24% of wall
- iter 16 the contract stops promising a ninth verb: `radar ingest` decided NOT PLANNED, and a brake checks every verb heading against the parser
- iter 17 `radar report` names every layer in the closed taxonomy; a zero-record layer renders `0` instead of vanishing, and the section states that a zero means unexamined
- iter 18 corroboration needs two distinct SOURCES, not two class labels on one; 0 of 16 confidences move
- iter 19 `radar scan` publishes how many records it applied; an empty register stops reading as a clean target on both surfaces
- iter 20 the on-disk record file becomes a documented read-only surface; both key tables are derived from the schema, so a renamed or removed key cannot leave the document quietly wrong
- iter 21 the `taxonomy` verb reaches the published `render.document` instead of carrying half a copy of its one-newline tail; stdout stays byte-identical at 1894 bytes and two dead imports go with it
- iter 22 the ingest door prints `examined N candidates in <inbox>` on every run that reads the register, and a vacuous inbox falls through to the single existing summary emitter instead of returning early, so a healthy empty run can no longer read as a crash to the research driver
- iter 23 `radar prd` declares each gap's detectability and, for an automated record, names where the register's own two-sided sample lives as `gaps/<ID>-*.json`; the 4 of 16 records the register declares undetectable no longer receive a story instructing a loop to reproduce them, and the automated/manual predicate collapses to one helper on `Check`
- iter 24 `Evidence.locator` gains the non-empty validator its twin `quote` already had, so a citation nobody can check can no longer pass `radar validate`; the rule stops at non-blank because that is the only one the schema, the ingest door and the out-of-band resolver all agree on, and two blank locators had been reading as ONE source and withholding a corroboration point from the derived confidence
- iter 25 a reader that stops reading no longer decides the exit code: any verb whose stdout pipe is gone returns 141 with an empty stderr instead of CPython's 120 plus an `Exception ignored` line, or exit 1 plus a `BrokenPipeError` traceback once the document outgrows the 64 KiB pipe buffer; `docs/CONSUMER_CONTRACT.md` gains an `## Exit codes` table whose backticked integers are asserted EQUAL to `cli.EXIT_CODES`, so a future code cannot ship undocumented, and 1 is reserved there for row 25
- iter 26 one glob dialect: `_iter_walked` matches register patterns with the product's own `_glob_regex` instead of `pathlib.Path.glob`, so a non-git target answers exactly like a git one -- an absolute glob no longer exits 1 with a traceback and zero bytes of document, an empty-string glob no longer fabricates `UNKNOWN -- Unacceptable pattern: PosixPath('.')` where the tracked path says `PRESENT`, and the 3.12/3.13 trailing-`/**` divergence `_glob_regex` was written to fix leaves the product with it; no flag, key, verb or exit code is added
- iter 27 register data only, `src/` and `tools/` untouched: GAP-014's `present_when` pattern loses its leading quantified class, which an unanchored `search` makes free by construction, and a census brakes the class across every record. 304.8 ms -> 35.4 ms on the same 53 files (8.0x, 29.2% of the register's whole regex budget), `radar scan .` median wall 0.73 s -> 0.49 s, and the emitted document stays byte-identical at 5,270 bytes. The prize is the superlinear axis: 40 lines of 3,200 class characters cost 8,766 ms before and 5.06 ms after (1,733x), and `**/*.js` is in this record's globs, so one minified bundle made the verb look hung rather than slow. Equivalence is argued from the regex, not from the corpus: the class contains no newline and `evaluate` publishes only `path:line`, so the reported line cannot move; row 37's merged-hits objection is unobservable because `evaluate` breaks after the first match per file. Rows 62 and 63 split out
- iter 28 the research contract stops publishing a rule shape the loader refuses: `not` takes a single `rule` object, not a `rules` list, and the document gains its first fenced example of every combinator, which is what converts the fix from a proofread into a decidable class. The brake extracts every rule object from the file's `json` fences and pushes each through `models.Gap.model_validate` -- the same call the ingest door makes at `tools/promote.py:167` -- and asserts the extracted kind set EQUALS `models.RULE_KINDS` rather than a hand-written count, so a kind added to the loader reds the document until it is exemplified and a legitimately reworded sentence does not. It starts two-sided by construction: before the fence existed the file's single `json` fence yielded 3 rule objects, all `content_matches`, all ACCEPTED, so a fence-only brake would have passed over this defect forever. Controls are synthetic `tmp_path` documents in the `test_iter22_behavior.py:358-381` idiom -- one carrying the old `not`+`rules` shape, which must be reported REJECTED, one carrying `not`+`rule`, which must be ACCEPTED, and one exemplifying only `content_matches`, which must fail the vocabulary equality by name. `src/`, `tools/` and `gaps/` are untouched and every verb's stdout is byte-identical
- iter 29 `radar show` gains one `## Detection` section, so the four of sixteen records `radar scan` can never score stop being indistinguishable from the twelve it can: the section names the check id and the verdict DERIVED from the evidence-bearing rule slots (`models.detectability`, one definition now shared by `show` and `prd` instead of two), and it keys the manual question on that derived kind rather than on the rule-slot list being empty -- a check declaring `applies_when` alone is schema-legal, loads, classifies manual, and silently lost the only actionable payload it carries. Byte-neutral outside the new section on 16/16 live records; a record with no check states so explicitly rather than rendering an empty section.
- iter 30 built this row-60 containment rule, earned `VERDICT: APPROVE` and `RESULT: PASS` over a green 1230-test working tree, and was then REVERTED at the clean-clone pre-ship gate: no ledger row for itself existed, so 4 ledger tests failed from a fresh checkout of its own ship commit while the working tree could not see it. Payload preserved as a patch and re-landed unchanged by iteration 31
- iter 31 re-lands iteration 30's row-60 containment rule unchanged, so a non-git `radar scan` can no longer follow a symlink out of the target root and quote lines as evidence about a tree that does not hold them; the ledger rows for 30 and 31 land in the same commit, since their absence is the only thing that reverted the predecessor
- iter 61 the ledger bound comes from the ledger: a row count, two contiguity claims and a real-git subset -- four literal iteration pins across two test files -- become a floor, a historical-prefix check and the product's own `unrecorded_ships`, so a ship commit can no longer red its own clean clone. Both judges already accepted the document and only the tests objected, so no invariant changed. This is the defect that reverted iteration 60 and left iterations 32-60 unshipped
- iter 62 re-lands iteration 61's ledger-bound relaxation, whose own new test file carried one premise assertion that was green pre-commit and red from every clone of its ship commit; the durability sweep now discovers the current iteration's own module instead of exempting it
- iter 63 a truncated file domain can no longer produce an ABSENT verdict: when MAX_SCAN_FILES cut the files a present_when rule searched and it found nothing, run_check returns UNKNOWN naming the cap and the total the domain held, so the one verdict of the five that claims safety can no longer rest on a search that stopped early; MANUAL is pinned unchanged by its own test, and radar scan . keeps every verdict and count unchanged because the cap is dormant on live data at 61 files against 4000, the only delta being three self-referential locator line numbers tracking the edited file
- iter 64 corrects the README's confidence rule to the pairwise form the code has used since iteration 18 -- a corroboration point needs two citations differing in both class and source document, so two labels on one URL earn nothing -- and adds a derived brake that measures the requirement through the real scoring.confidence() instead of pinning the prose, so the front door is required to name only the halves the scorer was measured to refuse to do without
- iter 65 `radar show` publishes the denominator the corroboration rule already keys on: one derived line under `## Evidence` naming how many DISTINCT SOURCE DOCUMENTS a record's citations rest on, computed through `scoring._source_key` so the printed count cannot disagree with the scorer. Measured at ship: 41 citations over 26 distinct sources, and 4 of 16 records hold more citations than sources -- `radar show GAP-015` renders three `###` blocks whose title, class, date and locator are byte-identical, all three one postmortem, and said nowhere they were one document. All four mismatched records sit at confidence 5, the ceiling, where the corroboration point `confidence()` withholds is invisible, so no surface distinguished five independent sources from five excerpts of two. The count feeds no score and enters no ordering, and the trailing sentence is byte-identical on every record, so the line is a denominator rather than a verdict. Row 42 shipped; its title named `radar report`, which prints no citation count -- the only site publishing one is `diff.py:122`
- iter 66 closes the last zero-byte verb: `radar list` reaches the published `render.document()` instead of appending its own one-newline tail, so a zero-record register gets exactly one newline and not zero bytes, byte-identical on the live register; and one brake asserts the guarantee over the bytes of every exit-0 verb derived from `build_parser()`, replacing a source-text census that was structurally blind to the append form
- iter 67 opt-in floor-gated verdict code: scan --exit-code spends the 1 iteration 25 reserved, exiting 1 when the target has a PRESENT finding whose evidence clears the confidence floor and 0 when it has none, with stdout byte-identical either way (measured 5,575 B and 15,132 B both ways) and stderr empty; the predicate is DERIVED from select_for_prd so the code and --json's below_floor cannot disagree, a below-floor PRESENT record is still DISPLAYED and reports 0, --prd and --exit-code are mutually exclusive because they answer one question with opposite codes, and a register that applied zero records refuses with exit 2 rather than certifying an all-zero census as clean
- iter 68 the prd hand-off speaks the consumer's dialect: `radar prd` and `radar scan --prd` emit their ordered story list under `stories`, so the one declared consumer's reader moves from `valid=False, total=0` to `valid=True, total=3, pending=('US-001','US-002','US-003')` on the same 3,693 bytes with no story object changed, and `docs/CONSUMER_CONTRACT.md` publishes the five emitted top-level keys behind a brake that derives them from the emitter instead of a hand-copied list (row 55). The frozen-default argument is withdrawn as unsourced -- `README.md` advertises the shape, never the key -- and dual-key emission is refused because a prd.json the loop writes back to can then diverge from itself and be counted stale
- iter 69 one matcher, not two: `_iter_tracked` and `_iter_walked` delegate their glob matching to a shared `_match_globs`, so the two copies of the match loop that had already drifted apart become one and an AST census finds exactly one module-level caller of `_glob_regex`; `root` is derived inside the helper rather than passed, so publishing evidence from outside the target root is unreachable instead of merely undocumented, and the `_glob_regex`-not-`Path.glob` rationale moves verbatim to sit with the matcher. Two spec predictions were measured FALSE and are withdrawn here: net lines 691 -> 692 (+1, not -23), and `radar scan .` is byte-identical at 5,310 B / sha1 `9dfac59e` rather than moving self-referential locators, because the register's locators into that file are :98, :100 and :115 while the first changed line is 230 -- an identity conditional on the edit staying below 115. The reviewer measured the containment stat as load-bearing by deleting it from a scratch copy, which then published a path that had been removed from disk. Three scan documents byte-identical, rc=0, stderr empty, no flag, key, verb or exit code added
- iter 70 one UNKNOWN gloss, true of both its causes: iteration 63 gave UNKNOWN a second cause that inverts on the safety axis -- the check DID run and will not vouch for a domain MAX_SCAN_FILES cut -- while all four non-test surfaces still published "the check could not be run", two of them in bytes rendered on every scan. checks.UNKNOWN_MEANING is now the one copy, the legend and the README cell are asserted equal to it, "## Could not run (UNKNOWN)" became "## No verdict (UNKNOWN)" derived from Verdict.UNKNOWN.value, and both causes are proved to reach that section with their own reason verbatim.
- iter 71 `radar taxonomy` publishes the fourth closed vocabulary: `## Record statuses` names every member of `STATUSES` with a gloss, and a brake introspects the module's vocabularies so an enforced-but-unpublished one reds the suite instead of surviving 70 iterations
- iter 72 the register honesty gate becomes testable and stops accusing correct data: `tools/verify_quotes.py` gains a keyword `fetch` seam and publishes `page_text`, so all four verdict labels are reachable from an offline suite for the first time -- it was the only tool in the repo with no test file naming it -- and a closed `INLINE_ZERO_WIDTH_TAGS` set lets a character-perfect quote spanning an inline `<code>` span verify instead of being reported NOT ON PAGE, the second instance of the one-sided-normalisation family the tool own docstring already records, with a block-boundary test pinning that no false merge was bought in exchange
- iter 73 the derived scores become reachable without pydantic: `agent_gap_radar.scoring` moves its `models` import under `TYPE_CHECKING` and replaces its one pydantic-only call with `_EvidenceOnly`, a frozen evidence-only stand-in, so the declared consumer -- which reads `gaps/*.json` as plain local JSON and had to rank by the STORED integers, naming a different top five from ours -- can call `priority`, `confidence`, `distinct_sources`, `strongest_source` and `promotion_options` instead of inventing a third ordering rule. Measured with pydantic blocked in-process behind a positive control: 80 identical / 0 different over 16 records x 5 functions, `rank` and `below_floor` id orders unchanged, every record still in exactly one view; `promotion_options` stops raising `AttributeError` on a record object with no clone method, which moving the import alone would have left as a landmine in the one function that tells a below-floor record how to earn promotion. Behaviour-neutral (16/16 against the pre-change module) and byte-neutral on all 8 verbs, no dependency, flag, key, verb or exit code added, and `docs/CONSUMER_CONTRACT.md` publishes the pydantic-free module set DERIVED from the same blocker the suite runs
- iter 74 corrects the published `priority` rule on all three surfaces that state it -- a weighted sum (severity x3 + frequency x2 + tractability x1, normalised to 0.0-10.0), never a product -- and derives the required spellings from the weight constants, so a weight change reds the documents instead of leaving a consumer to rank 12 of 16 records differently
- iter 75 built its increment (one slug rule, not two -- the duplicated title-to-identifier rule in `prd.py` and `tools/promote.py`), earned `VERDICT: APPROVE` and `RESULT: PASS` over a green working tree, and was then REVERTED at the clean-clone pre-ship gate: its PM stage was cap-killed after writing its spec but before it edited this file, so the ledger still ended at `- iter 74` and the ship commit turned `tools/roadmap_integrity.py` red with `iteration 75 ships in git but has no single done-ledger row` -- a defect CREATED by the commit and therefore invisible to every pre-commit stage. Recorded here by iteration 76 so a reverted iteration is explained rather than silently lost, which is how iterations 64 and 122 went missing; the implementation survives as a patch in that iteration state dir and has not been re-landed
- iter 76 row 67, the other half of iteration 15: `iter_files` memoises its ENUMERATION for the lifetime of one scan in a frame stack that mirrors `read_cache_scope`, so 38 calls over 11 distinct `(target, globs, exclude_tests)` triples cost 11 enumerations; the memo returns a fresh list each call so no caller can corrupt a later rule domain, and the key carries `exclude_tests` because 3 of the 8 glob sets are requested BOTH ways and all 3 answer differently (73 files vs 15, 90 vs 32, 73 vs 15) -- which also explains the 8-vs-11 discrepancy iteration 63 left open
- iter 77 row 76: the non-regression half of the published gate rule gets a machine surface -- `radar diff --json` emits a stable object (both domain sizes, added/removed ids with titles, and per changed record the nine `COMPARED_FIELDS` with old and new) so a consumer can ASSERT what `docs/CONSUMER_CONTRACT.md:371` prescribes instead of scraping a markdown report; the payload keeps `priority` and `confidence` as distinct fields, matching `scan_json` and `_list_json`, because a composite would launder the one distinction the register exists to preserve
- iter 78 row 61, the glob element the schema never looked at: `_validate_rule` now refuses a `globs` element that is not a `str`, so the loader can no longer certify a record that crashes the matcher -- `globs=[123]` passed `radar validate` at rc=0 with 27 bytes of stdout, then made `radar scan` exit 1 with an `AttributeError` traceback and ZERO document bytes, which iteration 26's match-nothing fix cannot reach because the crash precedes the regex. ONE limb, not four: the spec also proposed refusing blank, absolute and `..` globs, and the fix pass STRUCK all three as decided-against rather than deferred, because five committed tests in `tests/test_iter26_behavior.py` hold `/etc/**` and `""` schema-VALID through the CLI, all five asserting `radar scan` exits 0 (three via the `_verdict` helper, which also requires exactly one parsed finding) and a `..` glob was measured enumerating 0 files from a fixture whose parent held a marker, against 1 for the control. Row 61 therefore stays `open` with its title intact: any string is still a legal glob, now by decision rather than by omission. The sibling `pattern` check in the same function moved from truthiness to `.strip()`, so a whitespace-only pattern is refused while `""` keeps its existing message -- the two-sided control, and it refuses nothing the live register holds, with `radar validate .` unchanged at exit 0 and 28 bytes of stdout
- iter 79 row 77, the third vacuous-success door: `tools/check_locators.py` printed `N distinct locator(s), 0 broken` and exited 0 after checking ZERO locators, because `:58-60` `continue`s on any non-http locator and never counts it -- reproduced offline at rc=0 over a one-record register whose only locator is a bare DOI, with `curl` never invoked. The summary now states distinct, CHECKED, SKIPPED and BROKEN with `checked + skipped == distinct`; an all-skip register exits 2 with one `Error: ` line naming 0 checked of N; and a MIXED register still exits 1 and still prints its `BROKEN 404` detail, so the refusal tightened exactly where it was measured and not broadly. `main` gains a checker seam resolved at call time (the `fetch_fn or fetch` form at `tools/verify_quotes.py:220-223`), which gives the tool its first test file -- no test DRIVES it today and the only occurrence under `tests/` is one docstring in `test_iter24_behavior.py`. Locator SHAPE is untouched on purpose -- a non-http locator stays schema-legal and stays a SKIP, since hardening it is row 57's open question and contradicts row 44's stop at non-blank. Live register at ship: 120 records, 402 citations, 167 distinct locators, 0 of them non-http, so this is a latent fail-open in the honesty chain and not a live one. No `src/` module changed and no rendered byte of any `radar` verb moved
- iter 80 built the one-slug-rule collapse (`prd.py`'s `_slug` and `tools/promote.py`'s `_slug` onto the published `models.title_slug`), earned `VERDICT: APPROVE` and `RESULT: PASS` over a working tree running `2469 passed`, and was then REVERTED at the clean-clone pre-ship gate: `foundry preship` returned `verified: False` with `incomplete: False`, so the suite ran to a real RED verdict for 113 s inside a clean clone of the very commit whose working tree was green. That disagreement IS the gate working -- the increment depends on something present in the working tree and absent from the committed tree -- but the failing test still has no name, so this is a diagnosis owed and not a deletion owed. Not re-landed: the complete diff survives in that iteration's state dir as `SHIP_ATTEMPT.patch` (921 lines, 8 files, +745/-30) and the reverted commit `dababa3` is still reachable from the reflog. Recorded here by iteration 81 because `unrecorded_ships` is one-directional by design, so the ledger is the only place a failed iteration can be explained, and iterations 64 and 122 went permanently missing exactly this way
- iter 81 row 78, register data only with `src/` and `tools/` untouched, the same move iteration 27 made one shape further in: GAP-109 carried one 139-char pattern in BOTH `present_when` and `mitigated_when`, and because `checks.py` compiles every content pattern with `re.MULTILINE` its leading `^` matched at every line start, running two `[\s\S]*` lookaheads to end of file at each one -- quadratic in file size, measured at 2,798.7 ms of a single scan against 97.7 ms anchored with `\A`, 28.7x, at ~1.5 s/MB against ~0.04 s/MB for a median content rule. The anchor is provably verdict-identical rather than observed to be: the pattern is entirely zero-width, so if both at-or-after lookaheads hold at some line start p > 0 they also hold at position 0, and position 0 is always a `^` position -- the first match is therefore always at 0 and the reported line is always 1 either way. The DELIVERABLE is the census brake, not the character, because one field of register data is exactly what the next research promotion can reintroduce: the census refuses a line-anchoring `^` combined with a scan-to-end lookahead across all 506 shipped content patterns, and it is written against that SHAPE rather than against `carries a lookahead` because 5 patterns carry one (GAP-045, GAP-061, GAP-063, and GAP-109 twice) while only the 2 GAP-109 spellings are pathological -- the broad predicate would refuse three legitimate shipped records and land RED. `radar scan .` stays byte-identical at 19,688 bytes, which doubles as the guard against a new module accidentally crediting this tree with artifact-shape validation it does not implement and flipping its own GAP-014 verdict
- iter 82 row 79, the loop's own steering artifact: the `Next up` paragraph is this repo's only statement of ship order and it named three `shipped` rows, one of them (row 33) as the #1 strongest remaining candidate, so a PM reading the roadmap top-down was pointed at work iteration 73 had already landed. Rewritten to name only `open` rows with no restated figure, and `tools/roadmap_integrity.py` gains `ship_order_violations`, DERIVED from the Status column rather than from a keyword list, so the pointer becomes a ship precondition like the Done-ledger row instead of prose nothing reads. Non-vacuous by construction: a missing `**Next up:**` line and a paragraph naming zero rows are each their own violation, since a brake that passes over an empty parse is the failure `vacuity_violations` was added to prevent. No `src/` change and no rendered byte of any `radar` verb moves
- iter 83 row 10, the register's own time axis: `Evidence.date` was enforced on every citation and aggregated by nothing, so `radar report` gains `## Evidence age` -- each record whose newest citation is more than 365 days before the register's OWN newest citation date, which is the anchor because this repo has no clock and the bytes must stay committable. Placed between `## By layer` and `## Below confidence floor` so iteration 17's two byte-pins hold unedited. Display only: no age term reaches `confidence`, `priority`, `rank` or `below_floor`, nothing is dropped, and no rendered line says `stale`, because the honest repair is to go and check a source rather than pad a sound record with a fresher link. Threshold argued from the measured distribution and headlined by GAP-016, which ranks third on 401-day-old evidence while its own status already reads `partially-addressed`
- iter 84 row 16, the filter that was deliberately blocked on the never-drop rule and is now unblocked: `radar list --layer L` narrows the flat 120-line listing to one layer of the closed taxonomy, on the text surface and on `--json`. A filter IS the omission mechanism `VISION.md` names as the rule it protects, so it narrows the LOADED RECORD DOMAIN before `_list_rows` and row 26's single-pass `rank`/`below_floor` partition stays the only below-floor predicate -- proved at document level rather than by inspection: the 11 per-layer documents concatenate to exactly the unfiltered line multiset, so no record is dropped and none is duplicated, and the test derives its layer set from `taxonomy.LAYERS` instead of naming an id (row 28's landmine). An unknown layer is a user error, not an empty answer -- zero bytes on stdout, `Error: ` and exit 2 through the existing `_fail`, vocabulary DERIVED from `taxonomy.LAYERS` so no count is restated, and validated BEFORE the register is read so a typo does not depend on the register's health; a taxonomy-valid layer holding zero records INHERITS `document([])`'s one newline rather than answering that question privately again. `list --json` filters the same domain and gains NO top-level key: announcing the applied filter would re-baseline two committed key-set pins, one of them named `test_b7_list_json_stays_a_stable_object_with_no_new_key`, so it is recorded as new row 81 instead of riding along. `docs/CONSUMER_CONTRACT.md`'s stable-surface row lands in the same commit, because iteration 10 made that table derived from `build_parser()` and a new option reds the suite otherwise
- iter 85 row 82, the fail-open half of row 74: `tools/verify_quotes.py`'s `verify` took its network seam as `fetch: _FetchFn = fetch`, a default evaluated ONCE at definition, so a later `setattr(module, "fetch", fake)` -- the substitution three test modules already perform -- could not reach the one call `main()` makes (`verify(records)`, no seam). Measured offline before speccing, identity only and no request issued: patched attribute `is fake` True, `verify.__defaults__[0] is fake` False and `is` the real fetch, while the sibling `fetch_all` resolves at call time and carries the comment naming this exact hazard. The seam now resolves the CURRENT module-level function at call time through a one-line indirection (the parameter shadows the name, so `fetch or fetch` cannot express it), an explicit `fetch=` still WINS over a patched attribute, and the keyword is kept rather than renamed to the sibling's `fetch_fn` because 12 committed call sites pass it (AST-counted, the same walk that module's own census uses). Iteration 72's `test_b8_the_seam_default_is_the_modules_own_fetch` asserted the defect as a property (`default is verify_quotes.fetch`) and was INVERTED in the same commit rather than deleted, since a deleted fence is indistinguishable from a forgotten one; the new pin is that no `verify.__defaults__` entry is callable, which is the property that cannot come back. Production behaviour and `radar` bytes are unchanged -- no `src/` module, verb, flag, key or exit code moves -- and the new module installs the suite's socket tripwire so its own tests cannot pass while doing I/O, which 67 of 71 test modules still do not -- only 4 mention `socket` at all
- iter 86 row 34, the board's only byte-ALTERING row: `radar scan .` printed the scanning machine's resolved absolute root -- account name included -- as the markdown `Target:` line and as `scan --json`'s `"target"`, so the artifact `docs/CONSUMER_CONTRACT.md` points a CI gate at was reproducible only on the machine that produced it, and the byte-stability the contract sells held within one machine only. The two documents now echo the target the CALLER named, verbatim: no expansion, no resolution, no trailing-slash normalisation, and one accessor decides it so the two renderers cannot disagree. `target_name` and the `# Gap scan:` heading still derive from the RESOLVED path, so the document keeps saying what was scanned while it stops inventing where it lives; the top-level key set and its ORDER are unchanged, so the committed `TOP_KEYS` pin needs no edit; a `ScanResult` built with three positional arguments still renders the path it was given, which is what keeps iteration 70's three direct constructions meaningful; and the stderr diagnostic still names the resolved path on purpose. Byte-equality is now per INVOCATION rather than per target, stated honestly rather than hidden: a behavior scans one directory as `.` and as its absolute path and pins that the two payloads are equal key-for-key once `target` is dropped. Row 34's three-way choice is recorded as DECIDED in the row itself, with both rejected ways and their reasons, because a choice made silently is one a later iteration re-litigates
- iter 87 row 83, the fourth copy of one invariant and the guard that cannot see it: the JSON document tail collapses onto one published `render.json_document`, so all four `--json` surfaces reach the same implementation the markdown tail already has, and this iteration's own census adds a JSON rule keyed on the AST shape, beside the iteration-14 census that measures the markdown idiom (a `json.dumps(...) + "\n"` add) rather than on a literal -- because the four sites are byte-identical at only 3 of 4, so a literal census would report 3, stay silent, and read as healthy. `sort_keys=False` is `json.dumps`'s own default, so the four spellings enforced nothing; the one helper states the real guarantee (insertion order, never sorted) once, in the one place that can be cited. Emitted bytes are unchanged on every surface, which is the whole burden: `docs/CONSUMER_CONTRACT.md` points a CI gate at these payloads, so byte-identity on all four is the acceptance test rather than a green suite
- iter 88 row 84, the requiredness half iteration 30 left out: `docs/CONSUMER_CONTRACT.md` stops demanding four argument tokens the CLI makes optional (`radar validate [<repo>]`, `radar report [<repo>] [--floor N]`, `radar prd [<repo>] [--gap <ID>] [--project NAME]`), and the derivation from `build_parser()` grows a REQUIREDNESS rule proved two-sided in BOTH directions -- an optional argument documented as required AND a required one documented as optional -- with the option branch proved against a SYNTHETIC surface because no shipped option is required, so that half cannot be proved against the real parser at all. The contract had contradicted `README.md` about `--gap`, whose default IS the top-ranked gap, and `README.md` was the correct document and is untouched. The PM's own de-risking probe found the collision the spec would otherwise have handed a zero-headroom engineer stage: `tests/test_surface_contract_unit.py` carries the OLD cell text as a mutation anchor inside two `_replace_once(contract_text(), ...)` plants, so correcting the document REDS two committed tests -- measured, not predicted, by applying the edit in memory and running the four doc-brake modules (2 failed), then re-anchoring both plants and re-running (123 passed, rc 0, both files restored byte-exact). The two synthetic cells in the same module (lines 81 and 93) pass a literal to `documented_invocation` without reading the live document, so they are NOT anchors and stay. No `src/` module changed and no `radar` verb's stdout, stderr or exit code moved
- iter 89 row 56, the independence rule this register enforces INSIDE a record and publishes across none of them: `radar report` gains `## Source concentration`, keyed through the same `scoring._source_key` the corroboration point already uses, so a reader learns which sources several records rest on instead of reading a 120-row ranking as 120 independent findings. Measured on the register as it stood: 61 of 167 distinct sources carry more than one record, 102 of 120 records rest on a shared source, and the largest single source carries 13. A DISPLAY only -- no term reaches `priority`, `confidence`, `rank` or `below_floor`, because a source count read as a target is the Goodhart shape `distinct_sources` forbids by name, and shared sourcing is partly an honest property of a young field with few primary sources. The single-source half SHIPPED in the same section rather than being deferred, because `pm.md` behaviors 5 and 6 specify it and the Tester's oracle is that spec: the section's last non-blank line names the 9 records that cite exactly ONE distinct source, two of them resting solely on the 13-record source -- the compounding case, invisible on the shared-source table alone. It is a display under the same rule, and no row is left open for work this commit contains
- iter 90 row 85, the loop's own falsifiability proof joins the release path: `tools/verify_mutations.py` -- the only artifact proving this suite can FAIL, and an orphan whose token occurred in exactly one tracked file -- grows three pure pre-flight predicates over injectable read/exists seams (`anchor_defects`, `residue_defects`, `oracle_defects`) plus a `read_source` seam `main()` calls by bare name, and `tests/test_iter90_behavior.py` asserts all three SILENT against the live tree: 20 mutations, every anchor resolving exactly once, zero mutated forms on disk, every named oracle file present. So a refactor retiring one of the 12/7/1 anchors in `tools/promote.py`, `tools/verify_quotes.py` and `src/agent_gap_radar/cli.py` reds `uv run pytest` instead of disarming all 20 behind a message that reads as a tooling error, and a cap-killed plant left in a tracked file -- the median case at a 600.0s worst-stage median with 0.0s headroom -- is caught by the suite before `git add -A` can sweep it into a ship commit. `main()`'s fail-closed path becomes testable for the first time without touching a tracked file; its veto-all-on-first-defect semantics and its 20 subprocess runs are deliberately unchanged, so this brake claims the harness is ARMED and never that it still CATCHES
- iter 91 row 88, the one clause of the quality bar that nothing committed enforced: the repo is PUBLIC and 120 of its 224 tracked files are gap records written by an outside research pipeline from arbitrary primary sources, yet none of the five published refusal gates is a public-safety gate and exactly 2 of 78 test files carry banned-token literals, both scoped to the documents their own iteration edited -- so the other 222 files were covered by a per-iteration hand-rolled grep, which in iteration 90 reported CLEAN after ripgrep parsed `-E` as `--encoding` and the check's or-else fallback printed that word over zero opened files. `tools/check_public_safety.py` scans the `git ls-files` domain rows 18, 30 and 69 already establish, reports the count it scanned so a green result is legible, and fails CLOSED on the two ways a scan can be vacuous rather than clean: a domain of zero files and a file it cannot decode both return 2, which is this product's settled position after rows 22, 48 and 77. Four rules, all DATA, each proved two-sided against a planted sample AND against the live tree before the spec was written. It needs NO exemption list and NO self-exclusion, which is the whole design: every pattern requires at least one name character after its marker and the file never places one there, because each marker is held exactly once as a bare string literal terminated by that literal's closing quote -- not a member of the name-character class -- and `_path_rule` composes the pattern from that variable instead of inlining the marker beside the class, so the scanner is a member of the domain it scans and still reports zero, under one invariant a future editor must preserve: never inline a marker literal into a pattern or a sample. That is the trap iteration 06 hit live, when its self-scanning version failed on its own assertion line and had to be narrowed to two documents. Two rules were rejected on MEASUREMENT and are recorded in row 88 so they are not re-added: a windows-drive-path rule fires 467 times across 108 tracked files on correct data because an escaped newline inside a JSON verbatim code quote reads as drive-letter plus backslash plus name character, and an ascii-purity rule would red the 7 tracked files carrying non-ascii verbatim quotes, so the quote invariant outranks it. The email rule exempts the RFC 2606 and RFC 6761 reserved names, because an address at a reserved domain cannot identify a person and the two live occurrences are deliberate synthetic fixtures -- exempting the DOMAIN CLASS rather than the two FILES is what keeps this a rule instead of an allowlist. Real names and employer tokens are out of scope on purpose: a name list committed to a public repo is the leak it claims to prevent. No `src/` module, verb, flag, key or exit code moves, and no rendered byte of any `radar` document changes
- iter 92 row 90, the one clause of the published gate rule no machine payload could answer: `docs/CONSUMER_CONTRACT.md` points a CI gate at three honesty clauses -- a cited gap exists, is open, is above the floor -- and `scan --json`'s 12 finding keys plus `prd`'s 7 `sourceGap` keys answered two of them, because `scan.py` and `prd.py` never read `status` at all. Both payloads now APPEND it, passed through from the record and never defaulted in the emitter, so it agrees key-for-key with the `status` `list --json` has published since iteration 01; the scan paragraph and the prd section of the contract name it, which the committed every-emitted-key-is-documented check requires. The measurement that chose the shape: `radar prd . --gap GAP-016` exits 0 on a `partially-addressed` record at rank 3 and emits no signal of it, so this was live on the bridge verb rather than latent everywhere
- iter 93 row 51 bite 1, the dormant half the row itself demanded before any call site moves: `checks.required_literals(pattern)` extracts the literal set every match of a register content pattern must contain, or returns `None` when it cannot prove one, so a caller can reject a file before the regex runs. Five specified steps -- strip one leading inline-flag group; strip at most one FULLY-wrapping group after a balance check, which is what keeps both alternatives of `(foo)|(bar)`; split on depth-0 `|` outside a character class; take each alternative's leading literal run under a punctuation escape whitelist; and drop the run's last character when the next pattern character admits zero. Every ambiguity resolves to `None`, because a wrong literal skips a file and turns a PRESENT into an ABSENT, the fail-open inversion `checks.py`'s own header forbids, while a wrongly-refused literal costs one regex pass. Measured at 120 records over all three rule buckets: 361 distinct content patterns, 245 provable, 116 refused, 0 empty sets, 0 raises, and 0 unsound rejections over the 11 tracked `src/agent_gap_radar/` files. DORMANT by census -- nothing outside `checks.py` names it and `checks.py` does not call it -- so `evaluate`, `run_check`, `iter_files` and `_read` are byte-identical to iteration 92 and no verb moves a rendered byte, a key, a flag or an exit code. Bite 2, the call site, is the change that re-proves every scan verdict and does not fit beside this proof. Row 35's memo premise was re-measured on the way and REDUCED rather than refuted -- 12.8% duplicate evaluations against the 19.6% it was written from, on row 35's own metric -- and its Notes carry both that figure and why the spec's 2.9% is a different one
- iter 94 rows 47 bite 1 and 63, the work a scan does after its answer is already known: `evaluate`'s content loop leaves the file loop on the first hit when the kind is `content_absent`, the branch that returns `RuleHit(not found, [])` and DISCARDS its locations, so the boolean is settled at the first append and every later read and regex pass was unobservable. Measured at 120 records against this repo by iteration 94's scout A: `scan()` median wall 16,517 ms of which the content regex is 96.2% over 28,502 `(pattern, file)` passes, and 3,767 of those pairs are scanned for a `content_absent` rule already decided, costing 3,321.3 ms -- 20.1% of wall -- for one kind-guarded `break`. STRUCTURAL rather than empirical equivalence, which is why this and not row 51 bite 2: the return value depends on the loop only through `bool(code_hits or test_hits)`, monotone and settled at the first append, and `truncated_files` is computed from `domain_size` BEFORE the loop, so a cut domain still reports its full size and cannot be laundered into a clean-looking answer. The break is guarded on the KIND because `content_matches` ranks code-before-test over its whole domain, and a shared unguarded break would have silently truncated that location list. Row 63 rides along as its own row asked, dropped by iterations 27 and 93: a hit's line number is counted over a bounded range instead of over a copied prefix, deleting 10,555,378 bytes of string copies per scan with byte-identical line numbers. Row 47 stays `open` -- bite 2, the boolean-only / `exclude_tests` propagation shape, has already produced a fail-open in this module and must not ride along. Zero verbs, flags, JSON keys, exit codes or rendered bytes move; the committed two-sided check fixtures over every register content pattern are the gate
- iter 95 row 93, the front door that named none of the other documents: `README.md` gains one `## Documents in this repo` section naming all 5 sibling tracked Markdown documents by repo-relative path, each with a provenance word from a closed two-value vocabulary, and a tests-local oracle of pure functions over `(readme_text, tracked_paths)` holds the index two-sided -- nothing tracked goes unnamed, no named path is untracked or unresolvable -- with the expectation DERIVED from `git ls-files` through the seam `tools/check_public_safety.py` settled rather than hard-coded, because a hand-written list in a test is the same artifact as the missing list in the README and drifts the same way. Measured before speccing: 6 tracked documents, 0 of the other 5 named three ways over, `.md` occurring ZERO times in the committed README, and the quickstart pointing a CI gate at a `--json` object whose 407-line contract appeared in neither the README nor `radar --help`. Both directions are asserted because the live drift was one-directional OMISSION, so a containment rule alone passes the pre-change file, which named no paths at all, and proves nothing; the readers FAIL CLOSED on an empty tracked set and on a README with no index heading, since two empty sets agreeing with each other is the vacuous green this product has already ruled against on three other surfaces. ZERO files under `src/agent_gap_radar/` change, so no renderer, golden, exit code, payload or verb-surface pin moves. Row 94 carries the declined alternative with its measurements and its cheapest split, so the next PM inherits them
- iter 96 row 95, the contract this repo published twice and enforced nowhere: `[tool.pytest.ini_options]` gains `filterwarnings = ["error"]`, so any warning raised anywhere in the suite is a failure rather than a line nobody reads, and the glob character-class escape at `checks.py:148` -- whose docstring is where the stderr-purity contract is actually written down -- gains a direct proof that it is silent under `simplefilter("error")`, alongside a control proving the raw splice it replaces still warns. ZERO files under `src/agent_gap_radar/` moved, so no renderer, exit code, payload, verb surface or register record could shift, and the increment adds no user-visible bytes at all. The premise was measured before the pick rather than after: the whole suite already ran green under the stricter command-line `-W error`, so this landed with no warning backlog to clear and no `ignore:` escape -- and if a future dependency ever needs one, that is a dependency pin to weigh, not evidence this row was wrong. Two shapes were declined here on stated grounds: narrowing the filter to the product's own modules (it would not match the founding `FutureWarning`, which stdlib `re` raises) and removing `-q` from `addopts` (its brake is a pytest-in-pytest subprocess, and the fresh-clone wall was already 132.07s against a 120.00s advisory threshold, so the acceptance criteria forbade any subprocess test).
- iter 97 row 94 (`radar tags`), SHIP COMMIT NEVER LANDED -- recorded here because this ledger is the only place a failed iteration can be explained. The feature and its tests were GREEN: `reviewer_pytest.log` ran `3091 passed, 5 skipped`, exit 0, after the engineer and BEFORE the roadmap edit. What reverted it was its own Done-ledger row: raising the ledger maximum from 96 to 97 pushed `test_iter31::test_b8_the_floor_admits_a_later_well_formed_row`'s derived row to 100, which row 96's exact-length-two rule rejects, and the suite went to `2 failed, 3132 passed` with the failure naming `SequenceViolation(kind='not-two-digit', iteration='100')`. Both failures were in `tests/test_iter62_behavior.py` and neither touched the feature. The work is PRESERVED and re-landable rather than lost: `state/iter-97/reland/` holds `RELAND_tracked.patch` plus a copy of the tester's 630-line, 43-test module, md5-verified on both sides before the revert ran `git clean -fd`. Row 94 is therefore still `open` and is the strongest forward candidate now that iteration 98 has removed the ceiling that reverted it.
- iter 98 row 96, the ship PRECONDITION and the suite contradicting each other so that nothing could ship at all: `ledger_sequence_violations` judged a Done-ledger iteration number by `len(raw) != 2` and now judges it by canonical zero-padded form, `raw != f"{value:02d}"`, so iteration 100 and beyond are recordable while `2` and `0100` stay violations. This is a release-blocking fix rather than a feature: `unrecorded_ships` requires exactly one ledger row per shipped iteration, and iteration 97 was reverted -- green feature, green tester module -- purely because its own row raised the ledger maximum until a derived test row reached 100. Iteration 98's row is worse than 97's and that was measured before the pick, not after: at max 98 `test_iter62::_grown` reaches 100 by itself and `test_iter31`'s b8 row reaches 101, two violations against 97's one, so the red this iteration opens with IS the reproduction and the fix is what clears it. The predicate is pointwise identical across 1..99, which is the reason a release-unblocking change touches no existing assertion: the four kind-name sites and the pinned `SEQUENCE_VIOLATION_KINDS` tuple are untouched, and the published kind `not-two-digit` is kept on purpose because a minimum pad of two is what it always meant operationally and a rename would trade an unblock for a five-site surface change. Ordering was pinned as well as form, because a digit boundary is exactly where a text comparison would pass and a value comparison would not: `98, 99, 100, 101` is accepted and `100` then `99` is `not-ascending`.
- iter 99 row 51 bite 2, the call site of a prefilter this repo shipped DORMANT six iterations earlier and then never called: `evaluate`'s content branch asks `required_literals` for the literal set every match of its pattern must contain and, when one is proved, skips the regex on any read file whose folded text holds no member of it. The read still happens, so no file that is scanned today becomes unscanned and the `MAX_SCAN_FILES` truncation accounting is untouched. Sound in ONE direction by construction, which is the whole reason the extractor shipped without a caller: a missing literal is decisive, while a present literal and a `None` both mean run the regex, so every unprovable pattern costs exactly what it costs today and can never cost a verdict. The fold is memoised for exactly the lifetime of `read_cache_scope()` -- the invalidation-by-scope row 43 argues for -- so a scan folds a file once instead of once per rule and no lowercase copy outlives the scan that made it. The gate is DIFFERENTIAL rather than analytical, because the failure mode is the inverted signal `VISION.md` calls a regression regardless of how much cleaner the output looks: one target scanned with the prefilter live and with the extractor substituted to prove nothing must produce byte-identical documents, and the per-pair sweep over the live register must find no file rejected by a literal set whose pattern the regex would have matched -- with that sweep required to report a NON-ZERO rejection count, so a domain in which the prefilter never fires cannot certify it
- iter 100 row 39, the PRODUCT DECISION that row had been waiting on since iteration 17 and the fail-CLOSED rule it left published: the contract told a release gate to accept a citation only when the record `is open`, and `taxonomy.STATUSES` holds four values, so a gate implementing the published sentence refuses a TRUTHFUL citation of GAP-016 -- rank 3 of 120 at priority 9.0, `partially-addressed`, and glossed by the register itself as "the gap still bites". DECIDED: `partially-addressed` is citable. The rule is published as an EXCLUDE-list and never as an include-list of `{open}` -- `TERMINAL_STATUSES` enumerates the two statuses that ASSERT the work is already done and `citable_statuses()` derives the remainder from `STATUSES` at call time -- because doneness needs POSITIVE evidence, which is the invariant `scan` already enforces for an ABSENT verdict, and because a status a later research pass adds must not silently vanish from the build pipeline. Ordered by `STATUSES` and never by frozenset iteration order, which is hash-seed dependent for strings and would make the vocabulary non-byte-stable. Corroborated on the consumer side BEFORE the decision rather than after: the declared consumer's live gate already implements this exact exclude-list in code and defends it in a comment, while its own integration spec still publishes the `open` clause -- so the register was publishing a rule its only declared consumer had been forced to invent and contradict. Iteration 92 made `status` ANSWERABLE from both payloads and left the CLAUSE wrong; this is the other half. ZERO renderer, payload, exit-code, verb-surface and selection changes: `radar prd` stays status-blind, so the contract's own status-blind sentence stays true and no golden moves
- iter 108 row 97, the largest body of authored register text with no reader: `radar show`'s `## Detection` block gains one final `- Rationale: <value verbatim>` bullet, so the deep view a reader consults before starting work states WHY a signature is believed and what it can get wrong instead of only THAT one exists. Measured before the pick and not from a roadmap Note: the token `rationale` occurs ONCE across `src/` and `tools/` (its declaration at `models.py:140`) with zero `.rationale` accesses, while 119 of 119 records with a check carry a non-empty one -- 76,853 chars, median 645, min 78, max 1,206 (GAP-032). Shipped with its guard rather than without it, because the display limb creates a NEW coupling between authored register prose and the brief's block structure that nothing enforced: `models.Check` now refuses a rationale holding a newline or differing from its stripped form, which is a reproduction and not a precaution -- `line one` plus a newline plus `line two` validated at rc 0 through the real loader before this landed, and would have broken iteration 29's single-contiguous-block pin on a later iteration's watch. Both absences are DISPLAYED rather than dropped, the rule row 13 exists to protect: an empty rationale renders `none recorded` and the one checkless record (GAP-010) keeps its block byte-identical. The field stays optional at the door and no machine payload gains it, so no verb, flag, JSON key or exit code moves and the only rendered bytes that change are inside `## Detection`
